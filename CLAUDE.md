@agents/index.md

## Commits

Prefer single-line commit messages. If a change needs a longer body to explain it, it probably wants to be more than one commit (split by scope, e.g. one per `ts`/package-name).
