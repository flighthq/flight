# keyboard — Assessment

See [charter](./charter.md) for blessed direction.

## Recommended

1. **Document `transition.height` limitation** — `transition.height` is frozen at 0 before fire. Known web limitation, not fixable in web backend. Add a code comment documenting this for native host implementers.

## Approved

1. **Document `transition.height` limitation** [2026-07-02 · blanket "platform integration suite sweep"]

## Backlog

None.
