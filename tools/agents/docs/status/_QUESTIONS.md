# Builder questions ledger — surfaced, not blocking

Append here as you hit design decisions (also note each in the package's `status.md`). These do **not** block other phases — park the specific sub-task and continue. Seeded below with the judgment calls the pruned-core triage deferred (the keepers are already ported in `_port/`; these are the _not-clearly-keep_ items). At end of run, consolidate into a digest at the top.

---

## RECOVERY DIGEST — red tree → green (2026-06-25, per `_recovery-green-baseline.md`)

An interrupted run left the tree red (`npm run check` → 166 TS errors / 8 packages). Recovered per the reviewer's `_recovery-green-baseline.md`. **`npm run check` now exits 0; `tsc -b` = 0 errors.** Note: git is unavailable in this sandbox (the worktree gitdir `…/flight/main/.git` is not mounted), so intent was recovered from the directive + in-tree evidence, not git history.

Per cluster:

- **menu (151) — reduced to the flat command shape (Option A).** `MenuItemRole` is OPEN (`WellKnownMenuItemRoleValue | (string & {})` — the type form of the const; the assignment's literal `WellKnownMenuItemRole` is a value). `MenuItemTemplate` flattened to `{id?,label?,type?,role?,accelerator?,enabled?,checked?,submenu?}` (dropped `visible`/`onSelect`). `MenuBackend` pulled DOWN to host-electron's real 3-method seam (`setApplicationMenu→boolean`, `popupContextMenu`, `subscribeSelect((id:string))`). Stripped the rich OOP drift from `menu.ts` (`setMenuItem*`, `appendMenuItem`/`insertMenuItemBefore`/`removeMenuItemById`, `getMenuItemById`, `destroyMenuHandle`, `showContextMenuAt`, per-item `onSelect` machinery), removed the `visible` check from `buildWebMenuElement`, rewrote `menu.test.ts` to the surviving exports, dropped `visible` from `menu-templates(.test).ts`. **Deleted 4 now-orphan type files** (`MenuHandle`, `MenuItemHandle`, `MenuItemSelectEvent`, `MenuContextMenuOptions`) + their barrel exports; `MenuSignals.onMenuItemSelect` is now `Signal<(id: string)>`. host-electron now matches the seam with no changes. Detail in `status/menu.md`.
- **node/shape/render (4) — `DisplayObjectTraits extends NodeTraits`** (restored `enabled/kind/name` conformance; fixed the displayobject `canAddChild` variance). The 3 residual call-site errors are real `Node<Traits>` invariance (the `canAddChild` arrow is contravariant), NOT fixed by the trait change alone: `computeShapeLocalBoundsRectangle` now takes `Readonly<BoundsNodeAny>` (the canonical `HasBoundsRectangle.computeLocalBoundsRectangle` param type, which `Shape` satisfies), and `renderQueue.ts` casts the child-walk `current as NodeAny` (precedent: `viewport.ts` `as any`). This is a slight expansion of the directive ("make subtypes provide the fields") because the fields alone don't resolve invariance — flagging in case the reviewer prefers a different invariance treatment.
- **effects (2)** — removed `enabled` refs from `renderEffectValidation.ts` + its "skips disabled" test.
- **effects-canvas (1) — DEVIATION from the literal directive.** The directive said "rename → use `getCanvasRenderEffectSupport`", but that name was tsc's (circular) suggestion: the symbol is the missing const `CANVAS_RENDER_EFFECT_SUPPORT` referenced _inside_ `getCanvasRenderEffectSupport`. That function has **no test** (its registry test covers the other 3 functions) and the catalog exists nowhere → it is interrupted-run residue (an untested export would also fail `exports:check`). **Deleted the orphan function** instead. Flagging the deviation.
- **spritesheet (7)** — `sheet.atlas!` null-assertions in `spritesheetFrom.test.ts` (atlas nullable).
- **textshaper (1)** — vertical `TopToBottom` direction maps to `undefined` for `ShapeRunOptions`.
- **filters (1)** — `as unknown as BitmapFilter` cast.

Also (earlier, blessed Round-3 displayobject): restored Video setters `setVideoSmoothing`/`setVideoSource`; deleted `sdk/collision.test.ts`. Round-3 `Bitmap.sourceRectangle` removal stays DEFERRED per explicit user instruction (it is present and used by `spritesheet` + 4 backends).

---

## ROUND 2/3 DIGEST — 2026-06-25 (read this first)

Round-2 lost-source recovery + Round-3 displayobject decisions. Tree is green after recovery: `npm run check` exits 0, full suite **867 files / 9377 tests** pass (was 7842).

**Done this round:** lint-rule fix (the gitignored `assignments/` inbound tray is now excluded from lint/format, so re-staged `_port/` never breaks `npm run check`); camera's 6 lost 3D modules recovered (picking/culling/depth/basis/frustumCorners/intersection, 60 tests); and the **big one** — a lost-source recovery of **201 functions across 43 packages** + **~94 lost types in `@flighthq/types`** (the curation pruned the header layer too), all reconciled to green.

**R2-1 — DEFERRED, needs a package-boundary ruling (premise was off).** The dispatch said to move atlas-descriptor parsing out of `spritesheet-formats` into a new `@flighthq/textureatlas-formats`. But the atlas-descriptor parsing **already exists in `@flighthq/resource-formats`** (its files are all `textureAtlas{Packer,Starling,Libgdx,Aseprite}Parse` → `TextureAtlas`, plus shared `parseXmlDocument` XML utils that `spritesheet-formats` consumes). Meanwhile `spritesheet-formats`' own parsers (`texturePackerParse`/`starlingParse`/`cocosPlistParse`/`libgdxAtlasParse`) parse the same files **independently** to `SpritesheetData` (the animation layer). So there are two parallel parser sets. **Question:** is `resource-formats` simply the mis-named `textureatlas-formats` (→ rename it, and have `spritesheet-formats` consume it for the atlas-region layer instead of re-parsing)? Or is `resource-formats` meant to be a broader resource-format home with `textureatlas-formats` split out from it? Renaming a package the dispatch never named, and deduping two recovered parser sets, is a boundary decision I won't guess. **Recommendation:** rename `resource-formats` → `textureatlas-formats` (it is exactly atlas-descriptor parsing) and refactor `spritesheet-formats`' parsers to delegate atlas-region extraction to it, keeping only the animation/frame metadata. Parked pending your ruling.

---

## DIGEST — end of builder run 2026-06-25 (read this first)

The run completed all phases (0–4, the user-added resources elimination, and the user-added Phase 5–6). Everything below is **green**: `npm run check` exits 0, full TS suite **763 files / 7842 tests** pass, Rust core builds + tests pass. Questions are grouped by how much they need you.

**A. Decisions I made autonomously that you should confirm (forced/blessed, but worth a glance):**

1. **SDK barrel collision floor** rebaselined 4000 → 3200 (barrel now 3323 keys; the pre-curation 4196 was stale after the integration curation drop). Is the post-curation floor accepted, or do you expect the dropped ~1000 names back? (full item ↓ Phase 0)
2. **`tileset` is its own package** (you confirmed mid-run); `resources` fully eliminated into `image`/`font`/`video`/`audio` + `textureatlas` + `tileset`; the Rust crates mirror this split. (↓ Phase 2 / 2.9 / 5)

**B. Genuinely deferred — need a ruling or a dedicated pass (NOT attempted):** 3. **`textureatlas-formats` / `tileset-formats` content** — no atlas/tileset _file-format_ parser exists to populate them; TexturePacker/Starling/Cocos/LibGDX atlas parsing currently lives in `spritesheet-formats`. Move it out, or leave folded? (↓ Phase 2) 4. **3D pipeline build-out (Phase 4, parked by design)** — picking-first (consumes Phase-1 raycasting), `render-graph`-vs-explicit-passes, the "3D strictly additive" bundle gate, and the 8 candidate packages (`environment`/`instancing`/`picking`/`postprocess`/`shadow`/`skeleton`/`animation`/`gltf`). Full design questions ↓ Phase 4 (items 7a–7e). 5. **Rust function-level parity (Phase 5)** — structural crate alignment is done, but the port sits at **68.8% native-core parity (~530 unported fns)** plus pre-existing clippy debt. Closing it is a dedicated multi-session effort, not a builder sweep. Want one scheduled? (↓ Phase 5) 6. **Remaining surface-rs wasm methods (Phase 6)** — 10 beneficial color-matrix/kernel helpers exposed; ~23 remain (codecs needing `image-rs`, fingerprint family, per-pixel get/set, surface↔image bridges) — want them, and with what codec strategy? (↓ Phase 6)

**C. Phase-3 sweep (120 done, 251 parked):** the 251 parked items are per-package and live in each `tools/agents/docs/packages/<pkg>/status.md` (mostly "cross-boundary: needs `@flighthq/types`/SDK barrel edit" or "design decision"). Two **stale-assessment** findings worth noting: `camera`'s `assessment.md` was written against a surface that only exists in `dist/` (live `src/` is far smaller — re-review needed); `app`'s assessment had stale premises (claimed-orphan types that `lifecycle` uses; a cited `out`-param precedent that doesn't exist). The displayobject pruned-item questions (1–6 below) remain open from the seed.

---

## Phase 2 — decompositions (decisions made + one deferred)

- **`useragent` fold (Part A) was already complete; cleaned orphans.** The UA parsers already live in `@flighthq/useragent`, and `device`/`platform` web backends already import from it. The only residue was two orphaned directories (`packages/device-formats`, `packages/platform-formats`) holding stale `dist/`+`node_modules/`+`tsbuildinfo` with no `src/` or `package.json`. Deleted them. No question.

- **`textureatlas` extraction (Part B) — DONE. Tileset now its OWN package (user-confirmed 2026-06-25).** Extracting only the atlas from `resources` was impossible without a `resources ↔ textureatlas` cycle via `tileset`. I first folded tileset into `textureatlas` and flagged it; the user then confirmed tileset should be **its own `@flighthq/tileset` package**. Final layering: `image → textureatlas → tileset`. `tileset`+`tilesetFrom` now live in `@flighthq/tileset` (deps: entity, image, textureatlas, types). Consumers repointed; SDK barrel exports both; 93 packages valid; check green. **Resolved.**

- **`resources` ELIMINATED (user-directed 2026-06-25).** `@flighthq/resources` is deleted and split into `@flighthq/image` (image resources + constructors), `@flighthq/font` (font + font-resource), `@flighthq/video`, and `@flighthq/audio` — plus the already-extracted `textureatlas`/`tileset`. Every consumer repointed to the precise sub-package (each consumer was single-domain), SDK barrel + all tsconfig wiring updated, `resources` removed entirely. 93 packages valid; full check + 7832 tests green.

- **DEFERRED — `*-formats` siblings for the new asset packages (parked).** No file-format parser exists yet to populate `textureatlas-formats` or `tileset-formats` (the `tilesetFrom`/`textureAtlasFrom` constructors build from runtime image sources, not from authoring files; TexturePacker/Starling _atlas_ parsers currently live in `@flighthq/spritesheet-formats`). Creating empty `-formats` packages adds surface for nothing. **Question:** should atlas/tileset _descriptor_ parsing (TexturePacker / Starling / Cocos plist / LibGDX) move out of `spritesheet-formats` into `textureatlas-formats` (and/or a `tileset-formats`), with `spritesheet-formats` re-consuming them — or stay folded under spritesheet? Same open question now spans textureatlas + tileset. Not built; parked.

## Phase 5 — Rust parity alignment (structural done; function-level gap surfaced)

- **Structural alignment DONE: Rust `flighthq-resources` crate split to mirror the TS elimination.** Created `flighthq-image`, `flighthq-font`, `flighthq-video`, `flighthq-audio`, `flighthq-textureatlas`, `flighthq-tileset`; moved the `.rs` files; rewrote cross-crate `use`s (image ← textureatlas ← tileset); repointed dependents (`sdk` glob re-exports, `spritesheet`, and the `host-web`/`loader`/`texture` Cargo deps); deleted `flighthq-resources`; updated `[workspace.dependencies]`. `cargo build --workspace --exclude host-sdl` green; `cargo test` for all six new crates + sdk/loader/texture/spritesheet green; `cargo fmt --check` clean; `cargo clippy -D warnings` clean **for the six new crates**. `rust:conformance` now lists only the pre-existing `resource-formats`/`useragent` as missing crates — the six I added are resolved. Also fixed a surface-perlin Rust seed bug earlier (Phase 0). rustfmt+clippy components had to be installed in-session (`rustup component add rustfmt clippy`).

- **SURFACED — the Rust port has a large PRE-EXISTING parity gap (not closed; out of one builder pass).** `rust:conformance` reports **native-core unit parity 1171/1701 (68.8%)** — ~530 TS functions are unported across nearly every crate (e.g. log 51, notification 25, materials 24, mesh 21, node 21, storage 34, screen 24, scene-gl 71, scene-wgpu 75, …), plus two still-missing crates (`flighthq-resource-formats`, `flighthq-useragent`). This **predates this session** and is the domain of the dedicated Rust conformance program (the `rust` worktree + divergence map), not a builder sweep. My session's own function-level deltas (Phase-1 geometry/node additions like `disposeNode`/traversal/ ray3d; the Phase-3 per-package additions) fold into this same gap and are likewise unported in Rust. **Question:** do you want a dedicated follow-on pass to close the function-level Rust parity gap (large, multi-session), or is the structural crate-topology alignment sufficient for now? Attempting to port ~530 functions autonomously would be an enormous, hard-to-review change, so I stopped at structural alignment + verification.

- **SURFACED — Rust clippy `-D warnings` (`npm run lint:rust:core`) is PRE-EXISTING red.** Once clippy was installed it flagged lints across multiple crates (`flighthq-materials` `too_many_arguments` ×2, `flighthq-spritesheet` `derivable_impls`, and more — clippy aborts at the first crate). These are pre-existing (clippy was not previously runnable here) and untouched by this session; `too_many_arguments` in particular needs a signature-refactor judgment. Not fixed (out of scope, pre-existing). The six new crates are clippy-clean.

## Phase 0 — gates fixed-forward (resolved, surfaced for awareness)

- **SDK barrel collision floor rebaselined 4000 → 3000.** `packages/sdk/src/collision.test.ts` asserted the SDK barrel exports ≥ 4000 runtime names (pre-curation baseline note: 4196 across 83 packages). Merged `main` exports **3218** names across 85 re-exported packages — the integration curation that produced this tree intentionally dropped a large engine-core helper set (the same set Phase 1 partially re-adds). No mechanical barrel bug exists: the package list is complete, internal barrels are near-complete, and all genuinely-core packages score healthy (geometry 287, types 97, scene-gl 109, surface 92…). The low scorers are legitimately-small leaf/platform packages plus the still-stub `math` (2 — its full API is a Phase-3 maturation target). I lowered the floor to 3000 (with headroom under 3218) and updated the comment. **Question for the user:** is 3218 the accepted post-curation floor, or do you expect the dropped ~1000 names to return (beyond Phase 1's geometry/node re-adds)? The floor will be re-raised at end of run to the true post-build count.
- **surface-rs Perlin conformance fixed (no question — clean bug).** `flighthq-surface`'s `fill_surface_perlin_noise` used a per-channel seed of `seed + c*0x9e3779b1`, diverging from the authoritative `@flighthq/surface` scheme (`seed`, `+0x9e3779b1`, `+0x9e3779b2`, `+0x9e3779b3`) from the B channel onward. Fixed the Rust seed, rebuilt the wasm; all 82 surface-rs conformance tests pass.

## Seeded — displayobject pruned items (kept out of the autonomous port pending a ruling)

1. **`Loader` display object.** Builder's `displayobject/loader.ts` (the OpenFL `Loader` node) was dropped. Does it belong in `@flighthq/displayobject`, or is it superseded by `@flighthq/loader` (batch resource loading) plus a thin content-holder node? — touches the displayobject/loader boundary.

2. **Lifecycle signals + the `node → displayobject` hook.** `onAdded`/`onAddedToStage`/`onRemoved`/ `onRemovedFromStage` were constructible but emitted nowhere; the event originates in `node`'s `addNodeChild`/`removeNodeChild`. What is the hook — a callback slot on `NodeRuntime`, a registry the hierarchy invokes, or display-object-specific emission inside the hierarchy calls? (displayobject charter Open #3 — a real cross-package fork.)

3. **`Stage` property set.** Which OpenFL `Stage` fields are in scope: `frameRate`, `quality`, `scaleMode`, `align`, `displayState`, `mouseChildren`, `tabChildren`, `stageFocusRect`, `showDefaultContextMenu`, `contentsScaleFactor`, `fullScreenWidth/Height`? Some are render-relevant (frameRate/quality/scaleMode), others are focus/a11y/input concerns that may belong elsewhere or be deferred. Rule the set, then the keepers can port.

4. **`Bitmap` `pixelSnapping` / `sourceRectangle`.** Dropped alongside the props (note `smoothing` was kept). Are these real Bitmap features to restore, or superseded? `sourceRectangle` in particular overlaps atlas/sub-rect sampling that may live in `sprite`/`texture`.

5. **`Video` setters.** `setVideoSmoothing` / `setVideoSource` were dropped. Restore on the `Video` node, or route video source/smoothing through the media/render seam?

6. **DisplayObject traversal convenience.** `getDisplayObjectByName`, `getDisplayObjectStageDepth`, `isDisplayObjectOnStage` were dropped — but the generic `node/traversal.ts` (now ported) covers the underlying ops. Keep thin display-object-typed wrappers, or have callers use the node traversal API?

## Seeded — Phase 4 (3D pipeline, parked by design)

7. **The 3D build-out design pass.** `environment`/`instancing`/`picking`/`postprocess`/`shadow`/ `skeleton`/`animation`/`gltf`, `scene` promoted from stub, and `render-graph`'s reshaping of `render`. Blessed _in scope_ (structural fork G) but needs its own design session and must honor the "3D strictly additive" bundle gate. Do not build; surface and stop here.

   The decision points below are the design pass, expanded from the architecture target in `tools/agents/docs/render-architecture.md`. **Nothing here is built** — Phase 4 surfaces them and stops. Verified current state (2026-06-25): `scene`=4 src files (stub), `scene-gl`=39, `scene-wgpu`=37, `mesh`=4, `lighting`=8, `texture`=4, `camera`=3; the 8 candidate packages (`environment`/`instancing`/`picking`/`postprocess`/`shadow`/`skeleton`/`animation`/`gltf`) and `render-graph` do **not** exist yet.

   **7a. First buildable slice — `picking` (consumes Phase-1 raycasting).** Phase 1 landed the ray math (`createRay3D`, `getRay3DPointAt`, ray↔sphere/aabb/plane intersection in `@flighthq/geometry`). The architecture's "camera is a draw-arg" principle makes screen→ray trivial. **Questions:** (a) Is `picking` a standalone package, or does it fold into `scene` (as `pickScene`) or `interaction` (the 2D hit-test owner)? Recommend standalone `@flighthq/picking` mirroring `interaction`'s role for 3D. (b) API shape — `pickScene(scene, camera, screenX, screenY, out): SceneHit | null` returning node + barycentric + distance? (c) Acceleration — brute-force ray/triangle over mesh geometry first, with a per-`MeshGeometry` BVH slot as a later additive optimization? Confirm brute-force-first.

   **7b. `render-graph` vs explicit named passes.** The architecture doc commits to "passes are explicit named functions the app invokes; nothing auto-runs" (`drawSceneShadowMaps`, `bakeEnvironmentIbl`, `captureOpaqueSceneColor`, …). A `render-graph` package would _automate_ pass ordering/dependencies (opaque→transmissive, shadow-before-shade, IBL bake). **Question:** does multi-pass orchestration stay explicit (named functions in `render` + `scene-{backend}`, no new package) — or does `render-graph` become an _optional, additive_ convenience layer over those functions (never the only path, so the bundle gate holds)? This is the single biggest fork; recommend keeping passes explicit and treating any graph as an opt-in convenience, not a structural reshaping of `render`.

   **7c. The "3D strictly additive" bundle gate — how it is enforced.** Structural fork G's hard constraint: a 2D-only user pays **zero** 3D cost. **Questions:** (a) Which `npm run size` baselines guard it (the 2D `displayobject`/`sprite` import baselines must not move when 3D lands)? (b) Confirm the invariant that no 3D branch enters a 2D hot path — the mesh-material registry is a _separate_ WeakMap from the 2D quad-material registry (already designed), and `scene` never imports from `displayobject`. Add a size-gate assertion before any 3D package ships.

   **7d. New package set + boundaries (confirm each).**
   - `scene` (promote from stub): `Scene` root + `Mesh` node (`geometry` + `materials[]`) + transform-only `SceneNode` group, `prepareSceneRender` (world matrices, frustum cull via Phase-1 frustum helpers, pack light block). Mirrors the 2D `node`/`displayobject` split. Confirm scope.
   - `shadow`: `drawSceneShadowMaps` (directional=ortho, spot=perspective, point=cube, PCF). Standalone, or passes inside `scene-{backend}`? (A shadow camera is just a `Camera` at the light — no new node type.)
   - `environment`: IBL bake (`bakeEnvironmentIbl` — prefiltered specular + irradiance + BRDF LUT) + skybox. Standalone, or folded into `lighting`/`texture`?
   - `postprocess`: overlaps the existing `effects-*` packages (bloom/tonemap already there). Is `postprocess` a distinct package, or is 3D post just the existing `effects-*` pipeline applied to the scene target (the architecture doc's framing)? Recommend: no new package; reuse `effects-*`.
   - `instancing`: GPU instancing for repeated meshes. Standalone, or a draw-path option in `mesh`/`scene-{backend}`?
   - `skeleton` + `animation`: skeletal + keyframe animation for 3D. **Boundary with 2D animation** — `timeline` (MovieClip keyframes) and `tween`/`easing` already own 2D animation. Is there a unified animation core, or a separate 3D `animation` stack (sampler/channel/clip over `SceneNode` targets) with `skeleton` (joints/skin/inverse-bind) on top? This is a real cross-cutting decision.
   - `gltf`: the canonical 3D asset import (a loader/`-formats`-style package depending on `mesh`/`materials`/`texture`/`scene`/`animation`/`skeleton`). **Question:** scope — which glTF 2.0 extensions (KHR*materials*\* map onto the existing 20-material taxonomy; KHR_draco/meshopt; lights; animation)? And is it `gltf` or `scene-formats` under the `-formats` neighbor convention?

   **7e. Confirm the already-settled decisions still hold for the build-out:** camera + lights are draw-arguments (not scene members), with optional future `CameraNode`/`LightNode` + `findSceneCameras`/`findSceneLights` staying additive; `Texture` is the only 2D↔3D bridge (no bridge node types); `gl` + `wgpu` carry full 3D parity, Canvas/DOM stay 2D-only; build phasing is core-lit (20 materials, directional+ambient) → shadows → IBL → transmission/area as separate passes.

---

## INTEGRATION SYNC (worker → host) — 2026-06-25

Synced this worktree to builder's `head/` (`1acf96627`) by applying builder's exact `base→head` delta (405 modified + 494 added + 5 deletions), preserving this tree's ahead-of-builder work (all `scripts/`, untouched docs, and the binary assets the bundle did not capture). `npm run check` exits 0; full suite 9708/9708 pass. Two flags, neither blocking:

1. **`surface-rs` wasm conformance test is unverified here (environmental).** `surfaceWasm.test.ts` can't load `./wasm/surface_wasm.js` because `cargo`/the Rust toolchain is not installed in this sandbox, so `npm run build:wasm` can't generate the gitignored artifact. It is a build output (never committed), so it doesn't affect the source you commit — but the Rust↔TS surface conformance test is **not actually exercised** until someone runs `npm run install:rust && npm run build:wasm` in a toolchain-equipped env. Confirm that's acceptable for this commit, or build wasm before trusting that test green.

2. **Three of builder's docs arrived prettier-dirty** (`ISSUE.md`, `status/_QUESTIONS.md`, `status/menu.md`) — `format:check` failed on them until I ran `prettier --write` (whitespace only, no content change). Builder's reported-green tree should not have failed `format:check` on these, so either builder's format gate excludes them or they were written after builder's last `format`. Minor; flagging so the host knows these files were reformatted during the sync.
