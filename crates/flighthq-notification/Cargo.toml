[package]
name = "flighthq-notification"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "System notifications over a swappable web (Notification API) / native backend"

[dependencies]
flighthq-types = { workspace = true }

[dev-dependencies]
pollster = "0.3"
serial_test = { workspace = true }
