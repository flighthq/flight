//! Web notification backend — OS notifications + permission.
//!
//! Stub scaffold: a capability agent ports the TS `packages/notification` web
//! backend (`Notification` API + `Notification.requestPermission()`) into an
//! async `NotificationBackend` implementation here.

/// Installs the web notification backend as the active `flighthq-notification`
/// backend.
///
/// `TODO(host-web)`: empty scaffold — filled by the notification capability agent.
pub fn register_web_notification_backend() {}
