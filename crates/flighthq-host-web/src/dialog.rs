//! Web dialog backend — file open/save + message/confirm/prompt dialogs.
//!
//! Stub scaffold: a capability agent ports the TS `packages/dialog` web backend
//! (`<input type="file">`, `window.alert/confirm/prompt`) into an async
//! `DialogBackend` implementation here.

/// Installs the web dialog backend as the active `flighthq-dialog` backend.
///
/// `TODO(host-web)`: empty scaffold — filled by the dialog capability agent.
pub fn register_web_dialog_backend() {}
