[package]
name = "flighthq-resource-formats"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Resource-format parsing (XML document parsing; mirrors @flighthq/resource-formats)"

[dependencies]
# No flighthq-* dependencies: this crate is a self-contained XML parser. The
# only public type (XmlElement) lives here, mirroring the authoritative TS
# package, where @flighthq/resource-formats defines it (not @flighthq/types).

[lints]
workspace = true
