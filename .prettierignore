OVERVIEW.md

# Generated wasm-bindgen output for the -rs packages. The committed `.d.ts`
# contracts carry wasm-pack's own formatting; the rest is git-ignored anyway.
**/src/wasm/

# Agent tooling, orchestration scripts, and docs — mirrors the `tools/agents/**`
# ignore in eslint.config.ts so `npm run fix` (prettier + eslint) leaves the tree
# alone. Not package source; formatting it would fight the docs/dispatch layout.
tools/agents/
