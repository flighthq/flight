export interface AgxToneMapOptions {
  minEv?: number; // minimum exposure value (log2 domain floor). Default: -12.47393.
  maxEv?: number; // maximum exposure value (log2 domain ceiling). Default: 4.026069.
}
