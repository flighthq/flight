import { CustomShaderMaterialKind } from './CustomShaderMaterial';

describe('CustomShaderMaterialKind', () => {
  it('is the string CustomShaderMaterial', () => {
    expect(CustomShaderMaterialKind).toBe('CustomShaderMaterial');
  });
});
