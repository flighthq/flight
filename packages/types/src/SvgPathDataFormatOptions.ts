/** Formatting options for serializing a path as SVG `d` data. */
export interface SvgPathDataFormatOptions {
  /** Decimal places to retain; omitted to preserve full numeric precision. */
  precision?: number;
}
