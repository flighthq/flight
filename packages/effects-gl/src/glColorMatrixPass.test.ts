import { applyColorMatrixPassToGl } from './glColorMatrixPass';

describe('applyColorMatrixPassToGl', () => {
  it('is a function', () => {
    expect(typeof applyColorMatrixPassToGl).toBe('function');
  });
});
