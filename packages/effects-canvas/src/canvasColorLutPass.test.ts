import { applyColorLutPassToCanvas } from './canvasColorLutPass';

describe('applyColorLutPassToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyColorLutPassToCanvas).toBe('function');
  });
});
