import {
  applyBrightnessContrastEffectToCanvas,
  applyChannelMixerEffectToCanvas,
  applyColorGradeEffectToCanvas,
  applyGrayscaleEffectToCanvas,
  applyHueSaturationEffectToCanvas,
  applyInvertEffectToCanvas,
  applyLiftGammaGainEffectToCanvas,
  applyLookupTableGradeEffectToCanvas,
  applyPosterizeEffectToCanvas,
  applySepiaEffectToCanvas,
  applyWhiteBalanceEffectToCanvas,
  defaultCanvasBrightnessContrastEffectRunner,
  defaultCanvasChannelMixerEffectRunner,
  defaultCanvasColorGradeEffectRunner,
  defaultCanvasGrayscaleEffectRunner,
  defaultCanvasHueSaturationEffectRunner,
  defaultCanvasInvertEffectRunner,
  defaultCanvasLiftGammaGainEffectRunner,
  defaultCanvasLookupTableGradeEffectRunner,
  defaultCanvasPosterizeEffectRunner,
  defaultCanvasSepiaEffectRunner,
  defaultCanvasWhiteBalanceEffectRunner,
} from './colorGradeEffects';

describe('applyBrightnessContrastEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyBrightnessContrastEffectToCanvas).toBe('function');
  });
});

describe('applyChannelMixerEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyChannelMixerEffectToCanvas).toBe('function');
  });
});

describe('applyColorGradeEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyColorGradeEffectToCanvas).toBe('function');
  });
});

describe('applyGrayscaleEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyGrayscaleEffectToCanvas).toBe('function');
  });
});

describe('applyHueSaturationEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyHueSaturationEffectToCanvas).toBe('function');
  });
});

describe('applyInvertEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyInvertEffectToCanvas).toBe('function');
  });
});

describe('applyLiftGammaGainEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyLiftGammaGainEffectToCanvas).toBe('function');
  });
});

describe('applyLookupTableGradeEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyLookupTableGradeEffectToCanvas).toBe('function');
  });
});

describe('applyPosterizeEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyPosterizeEffectToCanvas).toBe('function');
  });
});

describe('applySepiaEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applySepiaEffectToCanvas).toBe('function');
  });
});

describe('applyWhiteBalanceEffectToCanvas', () => {
  it('is a function', () => {
    expect(typeof applyWhiteBalanceEffectToCanvas).toBe('function');
  });
});

describe('defaultCanvasBrightnessContrastEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasBrightnessContrastEffectRunner).toBe('function');
  });
});

describe('defaultCanvasChannelMixerEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasChannelMixerEffectRunner).toBe('function');
  });
});

describe('defaultCanvasColorGradeEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasColorGradeEffectRunner).toBe('function');
  });
});

describe('defaultCanvasGrayscaleEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasGrayscaleEffectRunner).toBe('function');
  });
});

describe('defaultCanvasHueSaturationEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasHueSaturationEffectRunner).toBe('function');
  });
});

describe('defaultCanvasInvertEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasInvertEffectRunner).toBe('function');
  });
});

describe('defaultCanvasLiftGammaGainEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasLiftGammaGainEffectRunner).toBe('function');
  });
});

describe('defaultCanvasLookupTableGradeEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasLookupTableGradeEffectRunner).toBe('function');
  });
});

describe('defaultCanvasPosterizeEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasPosterizeEffectRunner).toBe('function');
  });
});

describe('defaultCanvasSepiaEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasSepiaEffectRunner).toBe('function');
  });
});

describe('defaultCanvasWhiteBalanceEffectRunner', () => {
  it('is a function', () => {
    expect(typeof defaultCanvasWhiteBalanceEffectRunner).toBe('function');
  });
});
