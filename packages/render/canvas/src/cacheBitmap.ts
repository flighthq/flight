import type { CanvasRendererState, RenderNode } from '@flighthq/types';

export function updateCacheBitmap(_state: CanvasRendererState, _object: RenderNode, _force: boolean = false): void {
  // TODO
  // create and set object.cacheBitmap with RenderNode of new object
}
