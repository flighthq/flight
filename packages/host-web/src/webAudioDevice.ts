import { createWebAudioDeviceBackend } from '@flighthq/media/contract';

export { createWebAudioDeviceBackend } from '@flighthq/media/contract';

export const webAudioDeviceBackend = createWebAudioDeviceBackend();
