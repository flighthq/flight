import type { CanvasRendererState, RenderableData } from '@flighthq/types';

export function updateCacheBitmap(_state: CanvasRendererState, _object: RenderableData, _force: boolean = false): void {
  // TODO
  // create and set object.cacheBitmap with RenderableData of new object
}
