# Unbacked register — claims that reached the merged tree with nothing looking at them

**This register can only hold what someone noticed nobody looked at. A short register is evidence
about what we thought to register, not about how much is unchecked.** Read that line before any
count below it; without it the register is the best false-assurance instrument in the repository.

**What belongs here.** Any claim that entered the merged tree where no independent check ran on the
thing claimed — whether because nobody could run it, nobody thought to ask, or a green gate stepped
over it. That last case matters: **a gate that passes while skipping files every run is backed for
what it ran and silent about what it did not**, and "backed" is exactly the word that would absorb
those skips unnoticed.

**Two causes, different remedies.** **(A)** genuinely expensive or environment-bound — needs a
constraint lifted or a second environment. **(B)** cheap, unbacked only because nobody asked;
"I confirmed this test can fail" is the whole of this class, and reverting a fix to watch a test go
red costs seconds. **The set is selected by verification cost, which is not the same as every member
being expensive** — sort by cause before treating any entry as unfixable.

**Entries are marked at source with the fixed token `UNBACKED: <class>`**, one spelling, no
paraphrase — because the point is not that a reader is warned but that the set is countable.
An attestation with nothing unbacked says `UNBACKED: none`, so that "nothing unbacked" and "did not
consider it" stay distinguishable.

## Provenance of this file

Entries are supplied and owned by **integration**, who holds the operational record and is the only
role that observes what does and does not run at the boundary. This file exists in the repo because
**a record about the merged tree that cannot be found from the repo is not much of a record**, and
because it must outlive any one sandbox.

**Each row carries its own provenance.** `verified-at-tree` means someone read the artifact at the
integrated tree; `as-reported` means the row is the sender's self-report and nothing has looked at it;
**`unbacked-by-sender / backed-by-integration`** means the sender could not run the check in their own
clone but the boundary ran it on the combined tree — **which names who verified and who could not, and
is strictly more informative than either half.** Expect that shape whenever a sender is authoring
against supplied content rather than a tree they hold.
**The register's own contents are claims too, and they inherit every weakness the register documents** —
without the marker the table flattens checked and unchecked into one confident-looking grid, which is
the same flat-aggregate defect the scoreboard was designed to avoid.

**The trigger for adding a row is the final tree, not the parcel.** A claim is registered when it is
newly present in the final tree integration attests, whatever put it there — parcel, base move, or
integration's own edit. Mechanically: sweep `git log <last-attested>..HEAD` for baseline, corpus, perf
and mutation claims before attesting, not just the parcels applied along the way. **This wording is
deliberately identical in shape to the whole-repo gate rule** ("the gate runs on the final tree,
whatever put the commits there") **so the two cannot drift apart again.** The earlier, implicit
per-parcel shape of this trigger had the same blind spot the gate rule was corrected for this morning:
commits arriving by base move fire no parcel and no attestation event, so a claim that entered that way
was invisible to the register for as long as the per-parcel shape stood, with nothing to announce that
it had gone uncounted. A trigger with this blind spot does not produce a wrong count — it produces a
count that reads as complete while quietly missing a whole arrival path, which is worse, because the
number is the thing people trust.

## Entries

| # | Class | Commit (integration's post-apply hash) | Claim | Cause | Provenance |
|---|---|---|---|---|---|
| 1 | `capture-baseline` | `28b7d7f03` | builder4: swf-import baseline recapture landed on capture evidence alone (DOM/canvas/WebGL/WebGPU 0 changed, 0 failed; regression 0.00) | A | as-reported |
| 2 | `mutation-test` | **unresolved** | builder4: removing DOM Bitmap registration changed the Sprite | B | as-reported, **commit unresolved** |
| 3 | `live-acceptance` | `14f9e73de` | builder: byte-exact and hash-exact against the pinned 0.1.0 release | A | as-reported |
| 4 | `mutation-test` | `14f9e73de` | builder: mutation-tested variant fallback and merge-group behaviour | B | as-reported |
| 5 | `sender-environment` | `14f9e73de` | builder: 1.6 GB fetch, git status clean throughout | A | as-reported |
| 6 | `sender-environment` | `84051b0cb` | builder2: corpus 306 files / 1,166,258 bytes from a gitignored `.test-assets` — **flagged by them unprompted as clone-local** | A | as-reported |
| 7 | `untested-instrument` | `3dc27f69f` | builder: `packages/font-formats/src/openTypeTestHelper.ts` is itself untested while several test files depend on it — so a fault in it is agreed with, not caught | B | verified-at-tree `aa279c96a` |
| 8 | `perf-size` | `feat(effects-gl): report per-effect resolution so a passthrough stops reading as complete` (subject, not hash — see below) | builder2: `npm run size` run read-only over the effects-gl dispatch, effects/webgl −0.4%; no pin rewritten | A | as-reported |
| 9 | `capture-baseline` | `d7d8fbc8e` | builder2: restores support-matrix tick marks for `scene-morph`, `scene-skin-morph-compose`, `scene-skinning` and `scene-transparent` from fingerprints captured before the rename that swept them (`c0eeab24e`), never re-verified against what those scenes render today. The matrix is internally consistent — realization plus committed fingerprint — which is weaker than agreement. Whoever next runs `test:functional:regression` in the environment its baselines were captured in should check those four first. | A | as-reported |
| 10 | `capture-baseline` | `9c5b5ed4f` + `924be5c97` (both shared, on `origin/develop`) | `builder` **as reported to foreman — not derivable from the tree, since these arrived by base move through no parcel.** The two non-uniform-scale skinning scenes (`scene-skin-nonuniform-normals`, `shadow-skin-nonuniform`, webgl + webgpu) landed with their fingerprint baselines and 50 new `support-matrix.json` entries, captured in whichever environment ran the capture. The `support:check` and `fingerprint-source-hashes:check` gates both pass, proving the matrix is internally consistent with the committed fingerprints — weaker than agreement, same shape as row 9. Integration is barred from the regression and parity legs, so nobody in the integration path has compared these baselines to what the scenes actually render. | A | **BACKED — closed on a real-device run by `builder` at `4a83d3eb8`, 2026-08-11.** `capture --tool=functional --filter=scene-skin-nonuniform-normals --renderer=webgl,webgpu --fail-on-changed` → *2 captured, 0 changed, 0 failed*; same for `shadow-skin-nonuniform`. Four columns, current render byte-identical to the committed sha256, each scene's in-page CPU-prediction oracle also passing. **★ AND THE INSTRUMENT INTEGRATION ASKED FOR WOULD HAVE PROVED NOTHING** — integration's work order named `test:functional:regression`, but that leg is `validate`, which compares **`fingerprint`**, and these baselines carry **`sha256` only**. Integration verified both halves independently: the three baseline files expose `sha256` and no other field, `git log -p` over the row-10 files shows the string `fingerprint` has **never** appeared in either, and `captureValidation.ts:640-652` takes `committed === null` → `regressionUncovered++`, *"no fingerprint baseline — skipped"*, `result.skipped++`, `continue`. **So the requested run would have printed a clean pass having COMPARED NOTHING — a non-trial, and a green that could not have gone red.** `builder` caught the substitution and ran the leg that compares the field these baselines actually hold. *(Was: as-reported / read the commits and the baseline files, not the pixels.)* |

| 11 | `capture-baseline` | `62c5a6dc3` (`test(functional): baseline the two-skeleton skin palette regression lock`) | `builder`: the `scene-skin-two-skeletons` functional scene landed with its fingerprint baseline and 25 new `support-matrix.json` entries, captured in their environment. Integration is barred from the regression and parity legs, so nobody in the integration path has compared the baseline to what the scene renders. `support:check` passes, which is internal consistency with the committed fingerprint, not agreement. | A | **BACKED — closed on a real-device run by `builder` at `4a83d3eb8`.** `capture --tool=functional --filter=scene-skin-two-skeletons --renderer=webgl,webgpu --fail-on-changed` → *2 captured, 0 changed, 0 failed*. Same instrument correction as row 10: this baseline is `sha256`-only, so the regression leg integration originally named would have skipped rather than compared. **A pass here means the render is byte-identical to the committed hash — it does NOT mean the scene can detect what it was built to detect; see row 12, which is about this same scene and is the reason that distinction is not academic.** *(Was: as-reported / read the commits and the baseline file, not the pixels.)* |
| 12 | `mutation-test` | `c7024ccc8` (`fix(scene3d-wgpu): give every skinned draw its own skin palette region`) | `builder`: three mutations said to prove the arena, each reverted and verified identical to the commit — a non-per-draw base index giving 342px and 342px, single-row shader indexing making the narrow bar vanish at 0px, and a bind-pose-forced WGPU shadow pass giving mesh 618px / shadow 206px. Also the L7 repro: reverting `retireWgpuSkinArenaTexture` to `texture.destroy()` fails a capture with *"Destroyed texture used in a submit"* at coverage 0.00000. **Every one of these is measured in PIXELS through a capture leg**, which is the class integration is barred from by user constraint — so this is class A here, not the usually-cheap class B. | A | **PARTIALLY BACKED, AND THE UNBACKED PART TURNED INTO A DEFECT IN THE SCENE ITSELF.** `builder` re-ran all three at `4a83d3eb8`. **Reproduced verbatim (1 of 3):** `[shadow-skin-nonuniform] shadow pass off the CPU prediction: mesh 618px, shadow 206px, CPU predicts 617.1px`, by mutating `wgpuShadowMap.ts` to draw the shadow pass with an identity palette — pipeline and bind group left valid so the frame still renders and the oracle measures rather than dying; restored after, tree md5-identical to a pre-mutation copy held outside the repo. **NOT reproduced (2 of 3):** the `342px/342px` and `narrow bar vanishes at 0px` mutations. **★ Not merely unreproduced — MEASURED INERT.** Forcing the consumed base to 0 at both write sites (`wgpuSkinPalette.ts:183` and `:204`) and freezing the arena cursor each leave the frame **byte-identical** with the oracle passing. Cause, measured rather than guessed: the 80-joint skeleton is authored with `scaleX=1` on its bound joint and every other joint at default, **so its whole palette is identity and a wrong base lands inside an identical region** (base 0 + joint 70 = texel 280 = row 1 col 24, still inside it); and the frozen-cursor case is **masked by the deferred texture retirement from row L8** — growth retires instead of destroying, so a bind group recorded against the pre-growth texture still samples correct data for the rest of the frame. **The allocator itself is fine and is now measured, not assumed:** an allocator probe reported `FRESH base=0 joints=2 | FRESH base=256 joints=80`, so the two skeletons do get distinct bases and the multi-row path is genuinely exercised. **`builder` validated the instrument before trusting any null result** — a planted `throw` in `uploadWgpuSkinPalette` produced `✗ scene-skin-two-skeletons/webgpu MUTATION PROBE: uploadWgpuSkinPalette reached`, so the leg does go red and the function is reached; **the inert results are facts about the scene, not about a dead probe.** **CONSEQUENCE, and `builder` says it of their own scene:** the comment in `scene-skin-two-skeletons` claiming a disagreeing index would make the bar sample another skeleton's texels and render a wrong width **is not true as written — the width would not move.** Remedy (giving the 80-joint palette a distinguishing joint) changes the render and therefore the baseline, so it is **blocked on the recapture freeze** and is with foreman for a ruling. |
| 13 | `untested-instrument` | `c6242a039` (`fix(render-wgpu): retire replaced textures after submit instead of destroying them mid-frame`) | `builder`: two of the seven sites the retire-seam sweep fixes — `drawWgpuScale9Shape` and `drawWgpuRichTextWithOverlay` — have **no direct regression test, and cannot have one under the current harness**. They report writing a scale-9 resize test, watching it fail, and then measuring the cause rather than tuning it green: `createTexture` is called **zero times** across both draws under the mock device, so rasterization never allocates and the recreate branch is unreachable. They removed the test instead of leaving something green that exercises nothing — **which is the right call and is why this is an entry rather than a defect.** Those two sites are covered only transitively, through the shared seam's own three tests. The gap is not that the fix is wrong; it is that **the mock harness cannot distinguish these two sites being fixed from their being reverted**, so a future revert of either call site would pass every gate integration runs. | B | **OPEN — ROUTE NAMED, BLOCKED ON THE RECAPTURE FREEZE. NOT declined, and NOT blocked on an environment.** Integration originally filed this as needing a mock device that allocates, or a real-device leg it is barred from, and offered `builder` a declined-with-a-reason exit. **`builder` refused the exit and corrected the premise: the blocker has expired.** They have been running real WebGL and WebGPU captures all day in-sandbox — every number backing rows 10, 11 and 12 came off a real device — so *"no device that allocates"* is no longer true of the fleet, only of integration. **The route: a functional scene that drives `drawWgpuScale9Shape` / `drawWgpuRichTextWithOverlay` through a real recreate (grow the texture mid-scene), making the branch reachable with no mock at all.** Not built and not claimed. What it is actually waiting on is that a new scene needs a new baseline, which the recapture freeze forbids. **★ This row changed state twice for reasons that had nothing to do with the defect: first mis-scoped to integration's instrument, then mis-filed as environment-blocked when the environment existed elsewhere in the fleet the whole time.** *(Was: as-reported — cheaply convertible, but not by integration under the current constraint.)* |
| 14 | `mutation-test` | `206e25f29` (`fix(skeleton2d-formats): bound a bone parent against the bones already read`, and the two containment fixes below it) | `builder`: the three `spineBinaryParse.ts` containment fixes are each mutation-closed — broken, watched red, restored. | B | **BACKED — converted by integration at `206e25f29`.** Reverting **only** the parser (`git checkout quimby/base -- packages/skeleton2d-formats/src/spineBinaryParse.ts`) under the **post-fix tests** does not merely fail: the file **never returns** — 379.96s wall, `tests 0ms`, 24 tests unreported, killed at my timeout — against **14.32s / 20 passed** with the fixes in. So the containment is load-bearing and the tests do catch its removal. **★ THE STAGING ORDER DID NOT BACK IT AND I ALMOST RECORDED THAT IT DID.** The parcel names a test-before-fix commit (`48074ada9`), which is the artifact my class-B mechanism says to run — but that test file is **green at its own commit, 20/20, without either fix that follows it**. A test-first *commit order* is not a failing test, and only running it showed the difference. The conversion came from reverting the fix at HEAD, not from the staging. | 
| 15 | `perf-size` | `206e25f29` | `builder`: the DoS numbers — a 304-byte `.skel` with one rewritten varint previously returned **after 59 seconds or never**, and an inflated influence count burned **4.2s and several GB** before `Array.push` threw `RangeError`. | A | **PARTIALLY BACKED.** My revert-at-HEAD run (row 14) independently reproduces a **non-terminating** parse from the pre-fix parser, so *"or never returned"* is verified-at-tree. The specific figures — 59s, 4.2s, several GB, and the 304-byte input — are timing and memory measured in the sender's clone and are **not** backed by anything I ran. |
| 16 | `sender-environment` | `206e25f29` | `builder`: two corpus/sweep statistics — **7 of 348** generated corrupt skeletons produced graphs `validateSkeleton2D` rejects, and the removed IK dead-zone branch was **byte-identical across 1,536 parameter combinations**. | A | as-reported. Both are sweeps run in the sender's clone against generated inputs; my gates re-run the committed tests, which do not re-derive either population. The IK figure is the sole evidence that `852452a00` is behaviour-preserving — no committed test enumerates that space. |
| 17 | `mutation-test` | `5e196cc74` (`test(geometry): cover the slerp shortest-path branch with opposite-hemisphere inputs`) | `builder2`: `npm run unchecked` survivors in `slerpQuaternion` traced to one cause (every prior test slerped about `(0,1,0)`, so `bx`/`bz` were zero in both inputs); **all four arms now die, verified by targeted mutation after the fix**. | B | as-reported. Cheap to convert and I did not — my whole-repo run proves the two new tests **pass**, which is a different claim from their **killing the four mutants**. `unchecked` is the instrument that would close it. ~~Noting for the successor that this is the cheapest open row on the list.~~ **★ THAT COST CLAIM WAS WRONG AND I MEASURED IT WRONG BY ASSUMING IT.** I wrote *closable in seconds with `npm run unchecked`* without ever having run it. Measured on the next parcel: `npm run unchecked -- collision/src/shapeCollision.ts --quick` reports **321 reachable mutants, 36.6s baseline**, and blew past a **10-minute** timeout without finishing one file. **The whole-file sweep is not a class-B-cheap instrument; the targeted single-predicate revert is** (see rows 18–19: two arms, ~15s each). Correcting the technique rather than the row: to convert a mutation claim, **revert the exact predicate the sender names and run the sibling test** — do not reach for the sweep. |
| 18 | `mutation-test` | `0d86b767e` (`test(collision): pin every isValidObb field check with degenerate OBBs`) | `builder2`: five `isValidObb` field-check mutants previously survived — `Number.isFinite` on `halfW`/`halfH`/`rotation` each flipping `&&` to `||`, and both `> 0` extent tests flipping to `>= 0` — and all five are now caught. | B | **BACKED (SAMPLED 2 OF 5) — converted by integration at `7da32fd88`.** `shapeCollision.ts:662` `shape.halfW > 0` → `>= 0` fails **`rejects an OBB with a zero half-width`** (1 failed / 49 passed). `:659` `Number.isFinite(shape.halfW) &&` → `||` fails **three** fixtures (zero half-width, zero half-height, negative half-width). Both mutants die to the exact fixtures this commit adds; source restored and verified byte-identical to HEAD after each. I sampled two arms rather than all five — **so the row says SAMPLED, not proven-in-full.** Also corroborates `builder2`'s own correction that `unchecked` never writes the source tree: the worktree stayed clean across a 10-minute run. |
| 19 | `live-acceptance` | `7da32fd88` (`test(render-gl): pin the quad winding that requires culling off for 2D draws`) | `builder2`: proved on **real WebGL2** that Flight's 2D quad is a back face — **2304 lit pixels to 0** with culling enabled — in a scratchpad probe **now gone**. The commit pins the *consequence* (first indexed triangle is clockwise through the real projection), said to be mutation-verified by removing the projection's y-flip. | A | as-reported, and **the primary measurement is unrecoverable by construction** — the probe was deleted, and a real-WebGL2 pixel count is the capture class integration is barred from. **★ WORTH SEEING WHAT THIS COMMIT ACTUALLY DOES, because it is the right response to that:** the sender could not commit the GPU measurement, so they committed a **CPU-checkable proxy** for it (winding through the real corner order and projection). That converts an unrepeatable pixel count into a property a plain test run defends — my gates do back the proxy. What stays unbacked is the *link*: that clockwise-first-triangle is why culling must be off on real hardware. |

**Rows 11 and 12 come from one parcel and belong to different halves of the register's cost split, which
is the point of recording them separately.** A `mutation-test` row is normally class B — revert the fix,
watch a test go red, seconds of work — and the standing instruction is to convert rather than file it.
Row 12 cannot be converted: every one of `builder`'s mutations is measured as a pixel count through a
capture leg, and captures are exactly what the user constraint puts out of integration's reach. **The
class of a claim is not fixed by its label; it is fixed by what instrument would settle it.** A
mutation proven by a unit test is free, and the identical mutation proven by a rendered frame is not.

**Row 10 was corrected twice on the way in, and the first correction was itself over-corrected — which
is the more useful half.** As landed it opened `builder:`, an attribution **integration cannot derive**:
agent identity is not in git, every commit here carries the user's name and email, and parcels arrive
re-parented, so *the tree cannot answer who authored a line*. The first instinct was to delete the name
as a guess. **That was wrong, and row L5 in the ledger below is why** — it records the same authorship
from builder's own report of building `shadow-skin-nonuniform`. Foreman held evidence integration does
not. **Unverifiable-by-me is not the same as unfounded, and deleting a fact because I personally cannot
check it destroys information the file exists to keep.** The right move is the one the provenance column
was built for: keep the name, mark how it is known. It now reads `builder` as reported to foreman, not
derivable from the tree. Second, it cited `check-fingerprint-source-hashes:check`, which runs nothing. **One gate
has two names and the row had a third.** `npm run check` prints the label `fingerprint-source-hashes:check`
(registered in `scripts/check.ts`); the standalone npm script is `check:fingerprint-source-hashes`; the
implementation is `scripts/check-fingerprint-source-hashes.ts`. A name assembled from the file plus a
`:check` suffix looks right and matches nothing. **Any command quoted in this file should be pasted from
a run, never reconstructed** — a wrong command name is exactly the class of error that survives review,
because reading it does not execute it.

**Row 8 names its commit by SUBJECT, and the column heading is the reason it has to.** "Integration's
post-apply hash" is not a stable identifier: a rebase onto a moved `origin/develop` re-mints every
hash, and this row's original one (`ff08be351`) died within the hour of being written — the batch
carrying it was rebased before it ever reached the user. **That is the same mechanism that produced
the two `unresolved` rows above**, which are treated there as bad luck rather than as a defect in what
the column asks for. A subject survives every rebase, every re-parent, and every re-application; a
hash survives none of them. **New rows should carry the subject.** The existing hashes are left alone
because re-deriving them now would be the invented-substitute repair this file exists to prevent.

**Row 8 is the whole of what this batch left unbacked, and that is a change in the register's shape
rather than a quiet batch.** The same parcel carried a `mutation-test` claim — the class that has
occupied four of the eight rows — and it is absent above **because it was converted instead of
registered.** builder2 stated that disarming the re-registration seam fails exactly 2 tests and
disarming the resolution wire exactly 4, with no silence test moving either time. Both were re-run at
the integrated tree by short-circuiting each condition and restoring it: **2 failed / 279 passed, then
4 failed / 277 passed, against a 281 baseline, with the named failures matching the wires and nothing
else moving.** Cost: two package test runs.

**That is the class-B mechanism working, and it is worth naming because the register is where the
mechanism goes to die.** A `mutation-test` row is cheap to write and cheap to clear, so a register
that keeps accumulating them is not recording an unaffordable check — it is recording that nobody
spent the two minutes. **The correct steady state for class B is an empty column**, and any row of it
should read as debt rather than as inventory. Row 8 stays because a size figure measured in the
sender's clone is not reproducible at the boundary; that is a real (A), not an unspent (B).

**Where nothing recomputes, no number — and this file is the one place that rule inverts.** A claim
column records *what a sender asserted*, so its figures are the evidence and must be kept verbatim
with their provenance mark; stripping them would leave a register that cannot say what it registered.
**The rule binds the register's own assertions, not the claims it quotes** — U2 carried a repo-wide
guard count that nothing recomputed, and that figure is now a command. **Applying the rule to the
quoted claims instead would be the over-compliance direction: an absence that looks like discipline.**

**Two commit hashes in the table once did not survive re-reading. Row 2 remains marked unresolved
rather than repaired.** Its `56a794304` resolves to `fix(resources): decode embedded images as
bitmaps`, which touches `packages/image` and `packages/scene3d-resources` and no DOM file; no commit
in the merged tree matches the claim. **The claim may still be true — the reference is not**, and
inventing a plausible substitute is the one repair this file exists to prevent.

**Row 7's hash has since been resolved, and the failure mode is worth keeping.** `d77b5c2fa` was not
a valid object because it was a pre-landing SHA — `git am` re-parents on apply, and the commit that
actually landed is `3dc27f69f`; `git patch-id --stable` confirms the two are the same change
(`472e28b78b29afa0ce320af81c0dcefba3f08bbe`). It resolved only inside the clone that authored it and
nowhere else, which is exactly the trap this file's own naming rule exists to prevent — the rule did
not fail, the hash was simply recorded true of a tree and never re-derived after landing. Verified at
`aa279c96a`.

**Row 7 previously carried a long argument about which of two magnitude readings was in-population.
It rested on "exactly one commit in the merged tree has ever touched that file", which is false —
seven have — and both readings (*415 lines, 19 functions, 7 dependents* and *240, 13-of-which-3-
exported, 5*) are now superseded.** Measured at `328d5fdc7`, blob `6a9e0f5c3`: **590 lines, 8
exported functions, 9 dependents.** Recompute rather than quote:
`wc -l packages/font-formats/src/openTypeTestHelper.ts`.

**The magnitudes were never the entry.** The finding is that a helper several test files depend on is
itself unchecked, so a fault in it is agreed with rather than caught — true at every commit, while
the counts go stale on the next touch. Two things survive the correction: **a more precise reading
persuades independently of its currency** (*13 of which 3 exported* reads as better work than a flat
*19*, from an older tree), and **the second reading's real contribution was reaching the file from an
applied tree**, which is independent confirmation the author's clone-local claim could not supply.

**Entry 1 stays registered rather than reverted.** A marked unbacked claim is a known state; an
unmarked one is what this file exists to prevent.

**Entries 2 and 4 are backable on request** by landing the test before its fix as its own commit and
naming that commit **by subject line** — integration applies parcels as individual patches, so every
intermediate commit is checkoutable, and running one focused test there costs seconds. Two limits on
that route: hashes do not survive `git am`, so subject lines are the only durable reference; and a
red result shows *that* a test failed, never *why* — if it fails because the fix's scaffolding does
not exist yet, that is a compile failure wearing a test failure's clothes and the route does not apply.

## Landed defects — a third state beside unbacked and unbuilt

**An unbacked claim is unverified; a landed defect is verified and wrong, and already in the merged
tree.** An unbacked claim needs someone to **look**; a landed defect needs someone to **act** — but
that is not one bucket, **because some landed defects must not be touched by us at all.** Three
sub-states, or an entry lands in a someone-should-act bucket where nobody may:

- **FIXABLE** — a repair exists or can be written, and the only question is delivery.
- **MUST-NOT-BE-FIXED** — repairing it would destroy the thing it violates. **A ledger repair
  substitutes our reconstruction for the approver's text, which is the invariant itself.**
- **AWAITING A DECISION** — it is neither ours to fix nor ours to close: **someone outside the fleet
  has to rule on whether it stands.**

| # | Defect | Verified how | Sub-state |
|---|---|---|---|
| L1 | **CLOSED — and confirmed independently after a disagreement.** Both consumers now read `pack.verifiedFixtureFiles` and `tsc --noEmit` exits 0 — measured on the delivered base, not reported. *(Was: the base failed `npm run check` on its own, two typecheck errors where the fixture-stamp consumers were not carried with their rename)* | integration ran it against the base with nothing of theirs applied | **FIXABLE** — patch exists and is verified appliable; delivery, not authoring |
| L2 | A guarded `Decisions` line in a package charter was **removed rather than superseded**, and the mutation is in the delivered base | Baselined against the commit that introduced the guarded line: one violation | **MUST-NOT-BE-FIXED, AWAITING A DECISION** — report only; whether it stands is the user's call |
| L3 | Six `conformance/*-formats` suites (`awd2`, `gltf`, `md2`, `md5`, `obj`, `threeDs`) — **the entire scene3d format-importer family and nothing else** — each throw a `beforeAll` hook timeout at 10000ms, zero assertion failures, in the full resource-loader test run (`packages/scene3d-resources/src/{awd2,gltf,md2,md5,obj,threeDs}Load.test.ts`). **Timeout-class, not a correctness defect as reported.** Builder3's Stage 2 commit was suspected first; the control clears it. | **Bounded fact, settled:** builder3 ran the affected suite on the identical tree with `5973f3df6` reverted, **twice** — both runs produced the same 6 timeouts, 21 files / 105 tests passed, 9 skipped, 0 assertions, surviving removal of the suspected change. On the CURRENT tree, `5973f3df6` present and unreverted, the identical suite run with only `--hookTimeout=60000` (default 10000) passed **27/27 files, 114/114 tests, 0 failures** — setup latency is `>10s, <60s`, **not a hang, not an assertion defect, Stage 2 cleared either way this is sliced.** Integration independently reproduced a green→red→green flip on an *unrelated* suite (`conformance/swf`) on a fully unmodified tree within 20 minutes, same signature — evidence of environment-dependence in the general class, not evidence about these six specifically. **Cause, settled:** all six `beforeAll` hooks share the identical shape — `vi.resetModules()`, `vi.doMock('@flighthq/net/contract', …)` mocking `sendNetRequest`, `vi.doMock('@flighthq/scene3d-formats/contract', …)` mocking the format parser, then `await import('./…Load')` — **no network call, no filesystem read, no corpus fetch; `sendNetRequest` is a `vi.fn()` that is never invoked during setup.** The on-demand-corpus-fetch hypothesis is **ruled out** by direct read of all six files. The cost is module-graph reset plus re-import under remocking, run six times (once per suite) in the same worker. | **FIXABLE — cause identified: `vi.resetModules()` + dynamic re-`import()` under `vi.doMock` exceeds the 10s default hook timeout for this suite shape, most likely from contention when several such suites re-evaluate their module graphs in the same worker.** Not builder3's or this commission's to fix. Repair is either raise `hookTimeout` for these six (or the whole `scene3d-resources` project) in Vitest config, or reduce the per-file module-reset cost (e.g. share the mock setup instead of re-importing per suite) — a config change, not a source defect. |
| L4 | `camera-orthographic/webgl` fails functional capture — **the original finding was right.** ~~SUPERSEDED, marked false-positive on a fingerprint PASS at `3.67 ≤ 5`, with a *stale-subject-control* lesson attached.~~ **That correction was itself the error, withdrawn on the coarse instrument hours before that instrument was ruled non-authoritative for "did it change." The attached lesson goes with it — nothing about trees or machines was demonstrated here; it was an explanation fitted to a false premise.** Under `sha256` the render **does** differ from baseline (`sha-fail`), within fingerprint tolerance — exactly the class **L9** exists to hold. | *(struck history preserved above; superseding analysis lives at L9)* | **MOVED — folded into L9 as its best-documented instance, not carried as a standalone row.** Two superseded corrections is a sign the entry outgrew itself, not a reason to keep re-editing it in place. See L9. |
| L5 | Material `doubleSided` does not reach the shadow depth pipeline on **either** backend — a shadow-casting mesh authored single-sided casts an incomplete or missing shadow silhouette regardless of the material's `doubleSided` flag, on both WebGL and WebGPU. Found incidentally while `builder` built the `shadow-skin-nonuniform` functional scene: the test bar needed **both windings authored** into the geometry to work around the gap, rather than relying on the material flag. | Discovered as a required workaround, not measured as a standalone repro — `builder` needed geometry authored with both windings for the shadow pass to render correctly, which only makes sense if the shadow depth pipeline ignores `doubleSided` and single-culls regardless of the material setting. Cross-backend (both WebGL and WebGPU exhibit it), which argues for a shared shadow-pipeline assumption rather than a per-backend bug. | **FIXABLE — not urgent, not this arc's to fix.** Affects shadow silhouette correctness for any double-sided shadow caster (thin geometry, cards, cloth). Whoever owns the shadow depth pipeline on each backend should thread `doubleSided` into its culling state the way the main color pass already does. |
| L6 | `package-lock.json` carries two entries (a workspace declaration and its resolved link) for `examples/runners/web/src/wasm`, a path that is **untracked and absent from a fresh checkout** — `git ls-files` returns nothing under it, `test -d` fails. The repo's `workspaces` globs (`packages/**`, `examples/**`, `tools/*`) resolve it by filesystem lookup, so nothing in the checkout ever generates the directory the lock expects. **Latent, not yet triggered in the merged tree.** | Found and cross-checked by two builders independently. First clone (builder2): an `npm install` that had real re-resolution work to do (a genuine dependency change) re-walked the workspace globs, could not find the directory, and **delisted the two entries** — a small, easy-to-miss lock diff (1 insertion / 4 deletions) that reverting confirmed was not needed for the environment fix to work. Second clone (`builder`): the directory was equally absent, but `@types/jsdom` was already fully resolved in the lock at base, so the install only *materialized* existing entries and never re-walked the globs — a **non-trial**, not a counterexample, and initially mis-reported as one before the distinction was caught. **The trigger is now understood precisely: an install has to re-resolve (real dependency work) to exhibit it; a pure materialization cannot.** | **FIXABLE — not this arc's to take.** The dangerous install is exactly the one the npm-install-after-`package.json`-change rule already tells every agent to run, so this will keep recurring on ordinary dependency changes until someone either makes the `wasm` workspace real (present and buildable) or removes the stale lock entries. Whoever owns the `examples/runners/web` build should decide which. |
| L7 | `render-wgpu` destroys a texture **mid-frame while recorded bind groups still reference it**, if a texture is replaced (for example grown) during frame recording. The submit fails and **the entire frame blanks — not just the draw that used the texture.** The package already carries the safeguard for the other resource: `WgpuRenderState.retiredBuffers` (`packages/types/src/WgpuRenderState.ts:268`) is drained after `device.queue.submit` in `wgpuBackground.ts`, under a comment naming exactly this hazard for clip pops and grown particle instance buffers. **There is no texture sibling.** Manager ruled it gets its own entry rather than being filed inside the WGPU skin-palette arena fix that found it — *"a general defect that happens to have been found here… if it is filed only as part of this fix, the next person replacing a texture mid-frame re-finds it."* Same shape as the gate-trigger fix that had to be swept to its sibling the same afternoon. | **The DEFECT is verified-at-tree by integration at `50e1bc966`; the FIX is not, because it has not landed.** `retiredTextures` returns **zero hits across the whole repository**, and `git log 924be5c97..HEAD -- packages/render-wgpu packages/scene3d-wgpu` is empty — the arena work is in `builder`'s clone, not in merged history. So the missing-sibling condition is a present fact about the tree this file lives in, independently checked, not a report. The repair itself (`retiredTextures` added beside `retiredBuffers`, both destroyed post-submit; whole-repo check green, scene3d-wgpu 341/341, render-wgpu 206/206) is **as-reported by `builder`** and has passed through no integration gate. | **CLOSED — flipped on evidence at `fee58db7b`, exactly as the row required.** `builder`'s arena parcel came through integration and the safeguard is present *in the shape the row named*, which is what was checked rather than mere presence: `retiredTextures` is declared at `WgpuRenderState.ts:272` beside `retiredBuffers`, drained immediately after the buffers loop post-submit in `wgpuBackground.ts:144-147`, and `retireWgpuSkinArenaTexture` pushes instead of destroying. Whole-repo check and test green on the integrated tree. *(Was: FIXABLE — repair authored, not yet landed, with the flip conditioned on the parcel arriving.)* |
| L8 | **The L7 hazard has more instances than the remedy is scoped to.** The ruled prophylactic names one site — `scene2d-wgpu/src/wgpuScale9Shape.ts:131`, inside `drawWgpuScale9Shape`. A sweep of `.destroy()` on textures across `render-wgpu`, `scene2d-wgpu` and `scene3d-wgpu` finds **three live mid-frame instances of the same shape**, of which that is one: (a) `drawWgpuScale9Shape` `wgpuScale9Shape.ts:131`; (b) `drawWgpuRichTextWithOverlay` `wgpuRichText.ts:122`, an identical recreate-on-size-change destroy inside a draw function; (c) `bindWgpuTextureSourceTexture` `wgpuDraw.ts:91`, which destroys the cached texture whenever `cached.version !== image.version`. **(c) is the most reachable of the three and nobody has named it**: it is the payload-version-bump path, reached in production through `bindWgpuBitmapTexture` / `bindWgpuCompressedImageTexture` / `bindWgpuImageResourceTexture` from `wgpuTextureResolver.ts` and `scene2d-wgpu/wgpuTextLabel.ts` — so the repository's own invalidation doctrine is what drives it. Two further sites carry the shape but have **zero production callers today** (`bindWgpuVideoTexture` `wgpuDraw.ts:235`, `bindWgpuTexture` `wgpuDraw.ts:175` — test-only), so they are latent-unreachable rather than live. Two more are **not** the hazard and must not be swept in: the failed-copy `texture.destroy()` at `wgpuDraw.ts:175` and `:310` destroy a texture created in that same call that no bind group ever referenced. | **Verified-at-tree by integration at `fee58db7b`** — enclosing function derived per site, and reachability measured by counting production call sites rather than inferred from the name. That count is what separates (c) from `bindWgpuVideoTexture`: the two look identical in a grep and differ entirely in whether anything calls them. **Not proven: that each live site can be made to fire in a real frame.** The claim is that the shape and the reach are present, not that a repro exists — `builder` has a repro for the arena instance only. **Re-verified after the sweep landed, at integration's post-apply `949253114`**: the population was re-derived from scratch rather than trusted from this row, because the line numbers it names had all shifted under the patch — pinning to identity, not position. Every one of (a), (b), (c) now routes through `retireWgpuTexture`, and `grep` for `retiredTextures`/`retiredBuffers` outside `wgpuBackground.ts` returns **only the two type declarations in `packages/types/src/WgpuRenderState.ts`** — no inline push survives anywhere, which is the sender's claim independently confirmed rather than accepted. The two latent-unreachable sites (`bindWgpuTexture`, `bindWgpuVideoTexture`) were re-counted and **still have zero production callers**, so their exclusion stands on a fresh measurement rather than an inherited one; they remain unfixed and would carry the defect the moment anything wires them up. The two failed-copy destroys (now `wgpuDraw.ts:315` and `:620`, moved from `:175`/`:310`) were re-read and are still the not-the-hazard case. | **CLOSED — flipped on evidence at `949253114`, and the sweep was taken as the unit of work exactly as the row demanded.** `builder`'s parcel routes all three live sites plus their own arena through two shared seams (`retireWgpuTexture`, `retireWgpuBuffer`) exported from `wgpuBackground` beside the post-submit drains they feed, and extends the same treatment to **four buffer call sites nobody had swept** (`wgpuClipContours` ×2, `wgpuParticleEmitter2D`, `wgpuShapeMesh` ×2). Whole-repo check and test green on the integrated tree (1497 files / 17183 tests / 0 failures). **The remedy carries its own anti-revert protection**, which is the part worth keeping: a prophylactic fix has no bug report standing behind it, so the seam is documented at the definition with the blank-frame symptom spelled out, and three tests lock the deferral in. **Integration backed the mutation claim rather than recording it** — class B, and cheap for once: replacing both seam bodies with an immediate `destroy()` turns exactly the three named tests red (`retireWgpuBuffer`, `retireWgpuTexture`, `bindWgpuBitmapTexture` version-bump) and nothing else, then reverts clean. Contrast row 12, whose mutations are pixel-measured and stay unbacked — **the class of a claim is fixed by the instrument that would settle it, not by its label.** **Still not proven, and scoped deliberately:** that any of the six non-arena sites can be made to fire in a real frame. Only the arena instance has a demonstrated blank-frame repro; the rest are prophylactic-not-proven — shape and reach measured, never observed firing — which is how `builder` asked for it to be recorded and is the honest reading. *(Was: FIXABLE — applying the remedy to the one named site leaves (b) and (c) carrying the defect while the class reads as handled.)* |
| L9 | ★ **PROVENANCE VERIFIED — the seven split 3 genuine / 4 stale-`sha256`, not seven of a kind.** The L4/L8 mesh cluster showed the two fields can carry different provenance; builder3 ran the same `git blame`-both-fields check across all seven. **Genuine (3):** `bitmap-transparent-compositing`, `camera-orthographic` (formerly its own row, **L4**), `effect-crt` — fingerprint is shared-or-older provenance, so the render **does** differ from its committed baseline (`sha256` mismatch) within fingerprint tolerance, and the coarse gate reports them clean. This remains a live overclaim of the same kind as the 39-scene WebGL drift count, and worse: the 39 announce themselves, these do not. **Not this row (moved to L10):** `effect-displacement`, `effect-glitch`, `effect-scanlines`, `effect-screen-space-fog` — fingerprint is *newer* than `sha256`, the stale-record case this row always said would exonerate a scene if found. See L10. | Builder3's `git blame` on both fields, all seven scenes, same method as the L4/L8 mesh cluster. The 3 genuine scenes' fingerprint commits are shared with or older than their `sha256` commits. `camera-orthographic` carries the documented L4 history (three prior sha-fail reads, one incorrect fingerprint-pass correction, reverted). | **CLOSED — the bisect named the cause for all 3.** `camera-orthographic` and `effect-crt` each land on an exact adjacent-commit edge, both intentional correctness fixes (see L18). `bitmap-transparent-compositing` implicates an intentional two-step API migration but has no clean bisect pair across a transient broken-fixture window (see L18, bucket D — the one caveat left in this closure). Do **not** re-tune the fingerprint tolerance to catch these — that changes the instrument to make a result go away, the same move as re-capturing to clear a red, and nobody would know what the new number means afterward. The tolerance question is **the user's to rule, with the drift cause in hand** — which is now the case. |
| L10 | Four of the original seven fp-pass/sha-fail scenes — `effect-displacement`, `effect-glitch`, `effect-scanlines`, `effect-screen-space-fog` — are **stale-`sha256` records, not drift.** Same mechanism as the L4/L8 mesh cluster (fields from different commits), opposite direction: here the *fingerprint* is the fresher field. All four share a `fingerprint` refreshed by earlier same-day y-origin-convention recapture commits (`fd6d2769a751` / `afc0db33116a`), while `sha256` was left at a shared, older commit (`7383f696a743`, 2026-07-18) untouched by that recapture pass. The render has not changed since the fingerprint was taken; the coarse gate's fp-pass read is correct, and the sha-fail is inherited staleness, not silent drift. | Builder3's `git blame` on both fields for all seven L9 candidates; these four share the fingerprint-newer signature the L9 headline named as the exonerating case before verification. | **NOT-A-DEFECT, `sha256` NEEDS A REFRESH.** No render change to explain and nothing for the bisect to chase — these four come out of its target set. Whoever next touches these scenes' baselines should recapture `sha256` to match the already-correct fingerprint; not urgent, since the coarse gate already reads them correctly. |
| L11 | `bindWgpuTexture` and `bindWgpuVideoTexture` (`wgpuDraw.ts`, named test-only in **L8**) carry the same mid-frame-destroy shape as the fixed sites. ~~Recorded as unreachable from both `.` and `./contract` — zero exports on either lane.~~ **CORRECTED — that premise was wrong.** `render-wgpu/src/contract.ts` re-exports `wgpuDraw.ts` via `export * from './wgpuDraw'`; both functions **are** exported on the `./contract` lane and importable by any `@flighthq` package today. Only the public `.` lane is clear (`index.ts` does not re-export `wgpuDraw`). Accurate statement: **dead on the public lane, live on the contract lane, zero current consumers.** Builder caught this after being told to record the row: a name-grep on the two lane files returns zero for both symbols, which is a true fact about the text and a false conclusion about reachability, because a wildcard `export *` re-export is invisible to a name search — the same "name search finds candidates, following the re-export finds the population" shape as L8's reachability count, one level up. Builder also corrected their own earlier framing: this is not "goes live the moment anyone wires them up" (implying a deliberate export) — no wiring is needed, the hazard is one `import` away, not one export away. | Builder read `contract.ts` line 4 directly and confirmed both functions' export lines in `wgpuDraw.ts` (113, 217). Independently re-verified here by the same read before editing this row. | **DISPOSITION QUESTION REOPENED — escalated to manager.** Deletion may still be right (zero consumers on a pre-release contract lane is a real reason to remove), but removing a `./contract` export is a deliberate intra-SDK API-surface decision under the rename/restructure/remove rule, not incidental cleanup done "when someone is next in the file." `npm run reachability:check` is the gate that already has an opinion on `.`/`./contract` lane drift for cases like this and has not yet been run against this specific question. |
| L12 | **The split-provenance mechanism behind L9/L10 is a live tooling defect, not a historical artifact — three previously-separate defects (split fields, unrecorded acquisition path, unrecorded frame index) turn out to be one shape.** The capture pipeline can write `fingerprint` and `sha256` in the same baseline record from different commits — a partial write, not a corruption. L10's four scenes prove it is firing *today*: their `fingerprint`s were refreshed by this session's own y-origin-convention recapture commits (`fd6d2769a751` / `afc0db33116a`) while `sha256` stayed at a shared older commit (`7383f696a743`). The July mesh-cluster event (`e714387ac5b5` fingerprint / `ac45526bff52` sha256) is now understood as **the same defect firing twice**, not one historical event plus unrelated noise. | Same `git blame`-both-fields method as L9/L10, cross-referenced against this session's own commit list rather than an external date. | **PROVENANCE RECORDING LANDED (Stage 1), SPLIT-WRITE FIX PENDING A HANDOFF DECISION, GATE STILL CLOSED.** `builder` landed the `frames` provenance field, store wrappers, and `captureEntry` write (`d31ab19e3`), the `getObserveWarmupFrames`→`getCaptureWarmupFrames` rename, and the stale-dist-guard rationale comment — whole-repo check all green, `capture` 29/29, `tool-capture` 190/190. Also built a first pass at the split-write half themselves (`fd0ee759c`, "stamp the scene source the committed `sha256` was captured from") before the later dispatch split that piece out to `builder4` — a genuine collision, handled cleanly: `builder` handed `builder4` the commit and an explicit take-it/replace-it/drop-it choice and stopped touching it, rather than routing the conflict through foreman first. **"Untested end to end" is CLOSED for `builder`'s half, exercised by a dry run rather than left as a caveat.** Manager corrected the freeze reading first: "the freeze protects committed records, it does not forbid running the code." `builder` then ran `capture --root <scratch> --update-baseline` against a throwaway copy (the `capture` subcommand's own `--root` flag, `bin.ts:88` — no repo surgery needed): all five provenance fields populated, `sourceHash` genuinely differed between `webgl`/`webgpu` columns (real per-source-file work, not a constant), `targetKind`/`verifyPublished` matched the 450-column sweep exactly, existing `sha256` preserved unchanged (`0 changed`), provenance appended last so diffs stay minimal. Repo baseline `md5sum`-verified byte-untouched before and after. **`builder4`'s split-write fix has now landed too (`6ff5bc719`)**: shared predicates ensure atomic pair writes cannot bypass the uniform-fingerprint or known-blank-sha guards, mutation-tested (disarming the atomic check fails exactly 2 store tests). **Honestly scoped by `builder4` themselves**: production capture still uses independent (non-atomic) setters — nothing wired calls the atomic writer for real — so wiring it in would need capture+validation unification plus a ruling on preserving two-sample fingerprint stability, neither authorized. ★ **THE SPLIT-WRITE GUARD IS OVER-CONSERVATIVE AND THE DRY-RUN IS PAUSED PENDING A RULING.** ~325 columns carry both fields, ~117 `sha256`-only, ~6 `fingerprint`-only — **~27% partial by construction** (`capture` writes `sha256`, `validate` writes `fingerprint`, not every column gets validated), not decay and not a defect. Manager's correction to this row's original "written together or not at all" framing: the real cut is **PARTIAL** (one field, legitimate) vs **SPLIT** (both fields present, disagreeing provenance — the actual defect). The rule should be *if both fields are present, they must share provenance*, not a blanket both-or-neither. **`builder4` confirmed `6ff5bc719` implements neither form cleanly**: one-field records stay valid and same-field updates succeed (the 117/6 aren't directly rejected), but `setBaselineField` categorically refuses adding the counterpart field whenever the other already exists — it has **no provenance input**, so it cannot distinguish a legitimate partial-to-paired transition (a `sha256`-only column later validated, gaining a matching `fingerprint`) from a real split. **Any normal capture-then-validate sequence on one of the 117/6 partials would be incorrectly refused today.** `setBaselineCaptureEvidence` writes a pair atomically (the piece that *would* carry the provenance check) but is unwired. **RULED — narrow now, do not accept temporarily.** Manager: the staging was always "refuse only when BOTH fields carry provenance AND the provenance differs; absent provenance → allow" — the guard's current no-provenance-input refusal is a dispatch failure of the ruling reaching the implementation, not a design error of `builder4`'s. "Temporarily accept" buys nothing here because **123 columns are replenished by ongoing scene authoring** (6 of the 117 are scenes `builder` authored *today* — every new scene starts `sha256`-only by construction, since capture always precedes validation) — there is no interval where the over-conservative form is both live and harmless; the recapture is the next event and is exactly when it fires. `setBaselineCaptureEvidence` (the atomic-pair writer) stays unwired regardless. **Refinement to the stale-dist-guard asymmetry** ("wide costs a rebuild, narrow costs a day") — not universal: **when a guard sits across the critical-path operation it exists to protect, over- and under-conservative are both expensive**, and this is the counter-case; record both rulings together since they'd look contradictory read separately. ★ **STILL OPEN, BLOCKING IMPLEMENTATION: `builder4` found the ruled rule needs a data-model refinement, not just the stated logic.** A single shared/column-level provenance object cannot itself prove two independently-written fields agree — a later write of one field can replace the shared object while leaving the counterpart's provenance stale, recreating the exact ambiguity the guard exists to catch (today's asymmetry makes it concrete: `validate` records a top-level `sourceHash`, `capture` records `sha`-side provenance — already different granularity). **CONFIRMED by manager — build per-field. Two superseded rounds is a sign this entry outgrew itself; the confirmed design and its rationale move to L20, not carried further here.** The joint dry-run stays paused until the narrowed, per-field guard lands. Stage 2 (refuse cross-provenance comparison) remains held, unchanged, until most records carry the field. **Qualifies the standing "verify sha256, not fingerprint" constraint**: sha256 only answers "did it change" when the record's two halves share provenance — blame both fields before concluding drift on any scene whose baseline was touched recently. |
| L13 | **The "3 genuine" L9 classification rests on a discriminator not yet shown to discriminate.** `sha256` was ruled authoritative for "did the render change," but `captureEntry.ts:493` hashes **encoded PNG bytes** (`createHash('sha256').update(screenshotBuffer)`, written straight to `screenshot.png`), not decoded pixels — `CaptureColumnBaseline.sha256`'s doc comment claiming "raw decoded RGBA pixels" describes unshipped behaviour (the 2026-07-03 decode-before-hash item is still Approved-but-Open). An encoder change alone, identical image, would produce **exactly the fp-pass∩sha-fail signature the 3 genuine L9 scenes were certified on.** Not asserting L9 is wrong — asserting it hasn't been shown right by an instrument now known to answer a different question than the one it was trusted for. | Manager verified the hash call site and all eight `screenshotBuffer` assignment sites directly. `builder` ran the positive control: built the functional dist **once**, captured 6 skin/shadow scenes × 2 backends × 2 runs (12 columns), no baseline writes. All 12 **byte-identical run-to-run**. | **CONTROL PASSED, AND THE UNDERLYING DOC/BEHAVIOUR MISMATCH IS NOW FIXED, NOT JUST WORKED AROUND.** `sha256` is a stable identity on this tree — encoder drift is not producing the fp-pass∩sha-fail signature the 3 genuine L9 scenes were certified on, measured rather than assumed (10/12 columns reproduce their committed hash to the byte; the 2 that differ are stable/reproducible, not flaky; an encoder change would have moved every column, `webgpu` included, and moved none). **The 2026-07-03 decode-before-hash item is landed**: `builder2`'s `09d9ddb62` decodes the screenshot to RGBA in-page (`createImageBitmap` → canvas → `getImageData`) and hashes `<width>x<height>:` + the RGBA bytes, so `sha256` now measures what its doc comment always claimed. Dimensions are in the digest deliberately — RGBA bytes alone don't pin them, and a resize is exactly what a baseline exists to catch. Error-path/success-path handling is intentionally asymmetric (nullable on a page already crashed mid-error-report; throws on an undecodable success, since that means the capture lied about what it produced) and documented at each definition against future "fixing" into false symmetry. 6 tests, 2 mutation controls. **Honest limit, stated in the test file itself**: the decode is exercised by a test double, not a real browser — no Node PNG decoder exists, which is why the decode lives in-page at all — so unit coverage pins the *contract*, not whether a PNG *actually* decodes correctly; that only happens against a real browser, i.e. the eventual recapture pass. **This row closes for the doc/behaviour mismatch it named.** The broader mode/framing axis was L14's (separately closed by measurement). **L19 raised, then corrected, a webgpu blank-digest concern against a probe of the wrong design** — the actual committed mechanism measures non-blank on every channel including `webgpu`; see L19 for the full correction and what survives from it (a defensive guard being added regardless, and a real wiring hazard that was separately caught and closed). |
| L14 | ★ **THE ENTIRE 39-SCENE WEBGL DRIFT COUNT MAY BE CROSS-PATH, NOT DRIFT.** Builds on L13 (encoded-not-decoded bytes) with a second, larger axis: `screenshotBuffer` is assigned at **8 sites from three different encoders** (in-page canvas `toDataURL`, Chromium `page.screenshot()`, Playwright element screenshot), and **the branch is selected by runtime mode, not by the scene** (observe vs gate, whether the intercepted GL frame returned, whether the verifier published, `captureFrames > 0`). `page.screenshot()` captures the **viewport**; `locator('canvas').screenshot()` captures an **element crop** — different framing, not just different encoding. So `sha256` can change completely on an **unchanged render**, purely from taking a different branch. Two things beyond the original (principal's) finding: **(1)** silent fallback exists at **three** sites, not two — `:468/:471`, `:488/:491`, `:1034/:1037`, all `.locator('canvas')…catch(() => page.screenshot())` — a failed element grab silently substitutes a viewport grab and reports nothing. **(2)** the acquisition path is **not a recorded field**: `baselineStore.ts:19`'s `BaselineField` union (`'fingerprint' | 'sourceHash' | 'sha256'`) has nowhere to put it, so a record cannot say which mode produced its hash. | Manager verified all eight `screenshotBuffer` assignment sites and the three silent-fallback sites directly at `5b620df58`. Playwright version drift is **ruled out** by measurement — pinned at 1.61.0 since `2b0269341` (2026-06-17), only two commits ever touched that line, both 1.61.0, so the encoder binary has not moved across the baselines' lifetime; the axis that remains is entirely which branch ran. `builder` answered the mode-lookup **by measurement, not just by reading the code**: writer (`--update-baseline`) and comparer (no flag) are the same command/mode, differing only in a flag read *after* the hash computes at `:493` — cannot change the encoder branch. Validation never touches `sha256` at all (zero hits in `captureValidation.ts`; fingerprint-only), so no writer/comparer split exists to be cross-path about. They also directly exercised `captureFrames` (the flag-sensitive `webgl` toDataURL-vs-crop discriminator manager named) — 6 scenes, with and without `--frames`, 12 columns, all byte-identical — because **functional scenes always register a verifier**, and the verifier-published-dataUrl branch (`:445-446`) wins before the flag-sensitive branch is ever reached. | **CLOSED FOR THE POPULATION THAT MATTERS TO THE CURRENT COUNT — the 39 and the 3 genuine L9 scenes stand.** `builder3`'s hold is lifted; the bisect resumes. **Residual RESOLVED BY MEASUREMENT, not left open:** `builder` instrumented all 450 functional columns directly (gate mode) — **zero reach `:484`** (every WebGL column has `verifyPublished: true`; the 43 `dom` columns take their own dedicated branch at `:477`, excluded from `:445` by `backend !== 'dom'`). A related, separately-raised timing question (`captureBrowser.ts:247`'s `gpuVerifying` gate, which freezes capture at a possibly-different animation frame for any column whose `targetKind` is neither `'webgl'` nor `'webgpu'`) was checked exhaustively too: the full 450-column partition — `canvas`+`dom` 125, `webgl`/`targetKind:null` 113, `webgl`/`targetKind:'webgl'` 54, `webgpu` 158 — sums to exactly 450 with no remainder, and **all 238 frames-sensitive columns measured byte-identical between `frames=0` and `frames=1`.** The mechanism is real in code but changes no artifact anywhere in today's functional suite (bounds the consequence, does not prove the mechanism absent — an animating canvas/dom scene would be the closing test, none found/built). Both checks used a positive control (confirmed the discriminator actually moved per-row) before trusting the null. **Still real, still unchecked, still reference/example pages, not functional:** a page that registers no verifier at all falls through to the flag-sensitive encoder pair (`--frames` genuinely selects a different encoder there), and the silent `.catch()` fallback (`:468/:471`, `:488/:491`, `:1034/:1037`) still exists — the eventual fix (record the acquisition path, refuse cross-path comparison, make the fallback loud) is L12's staged remedy, in progress. |
| L15 | ★ **WITHDRAWN IN FULL — the mechanism is unreachable for functional scenes.** ~~Narrowed by builder's answer — their two L13/L14 control columns are cleared as real drift, but the 3 genuine L9 scenes are STILL open on this mechanism and unchecked.~~ `captureEntry.ts:1183` — `VERIFIED_CAPTURE_TOOLS = new Set(['examples', 'functional'])` — makes `verify` true **by constant** for the functional suite, so the `dataUrl`-published branch at `:445` (`else if (dataUrl && backend !== 'dom')`) catches WebGL **two arms before** the `captureFrames > 0` branch at `:484` is ever evaluated, for *any* functional scene. `--frames` is inert here across the board; `fingerprint` and `sha256` read the same artifact after all. **L9's 3 genuine scenes need no re-checking on this axis — they stand exactly as previously measured.** Manager's withdrawal of builder's original reading on their two columns (see the struck text below) lapses in turn, since it depended on this now-closed path — builder's original "real render change" reading stands unless something else displaces it. Manager: "principal read a branch guard correctly and never traced arrival; I read a token correctly and never traced whether it was the only route. Same error, opposite ends." — a fourth instance of the day's standing rule: **a condition you have verified tells you nothing until you know what reaches it.** `bin.ts:180` defaults the **`capture`** subcommand's `captureFrames` to `0`; `bin.ts:201` floors the **`validate`** subcommand at `Math.max(1, …)`. `captureEntry.ts:484` branches on `backend === 'webgl' && captureFrames > 0`. So a **fingerprint** written by `validate` (frames ≥ 1) compared against a **hash** written by `capture` (frames 0) is **cross-branch by construction** for `webgl` scenes without a registered verifier — this is what could reach L9's `fp`-vs-`sha` comparison, not the sha-vs-sha 39 (L14, unaffected by construction). **Builder's `scene-skinning`/`scene-skin-morph-compose` columns are cleared, three independent ways:** measured byte-identical at both `--frames=1` and no-`--frames`; `verifyPublished: true` in observe mode, which takes the `dataUrl`-published branch (`:445-446`) upstream of and independent from the `captureFrames` discriminator at `:478`, so the frames-sensitive pair is never reached; and their comparison is **`sha`-vs-`sha`** (both `hash` and `baselineHash` are `sha256`), not `fp`-vs-`sha`, so the mechanism never applied to them at all. **Methodological note, worth keeping:** the "only `webgl` differed" signature match does **not** discriminate real-drift from acquisition-path, since the branch is gated on `backend === 'webgl'` under either story — builder correctly declined to let the coincidence settle it and ran the direct checks instead. | Builder ran the two questions as direct measurement (frames-0/frames-1 byte comparison) plus a code-path read (`verifyPublished`, `verifyTargetKind` from observe-mode diagnostics) plus a `status.json` field check — three independent confirmations, not inference from one signal. Manager's withdrawn "real render change" read on those two columns is restored. | **L9's 3 GENUINE SCENES STILL UNCHECKED ON THIS MECHANISM — builder authorized to run the same observe-mode `verifyPublished` lookup on `bitmap-transparent-compositing`, `camera-orthographic`, `effect-crt` next (≈1 minute each).** `verifyPublished: true` for a scene rules the mechanism out for it (same reasoning as above); `false` means it genuinely is cross-branch by construction and that scene's L9 status needs re-deriving on a same-frames comparison before the bisect trusts it. Row closes when all 3 report. |
| L16 | ★ **WITHDRAWN — depended entirely on L15's mechanism, which is unreachable.** ~~`test:functional:smoke`'s own construction reproduces the L15 mechanism on every run, independent of how L15 resolves. The script is `capture --tool=functional --frames=1 --fail-on-error`... the remedy and the defect are the same line.~~ `captureEntry.ts:1183`'s `VERIFIED_CAPTURE_TOOLS` constant makes the `dataUrl`-published branch (`:445`) catch before `:484` for *any* functional scene, so `--frames=1` on `test:functional:smoke` never reaches the encoder/framing branch this row worried about — the flag is inert for the population `smoke` actually runs. **The residual L14 already named (a page with no registered verifier at all) is the live version of this concern, not this row** — L14 stands as the place to track it. | See L15 — same withdrawal, same cause (`captureEntry.ts:1183` verified directly by manager at `5b620df58`). | **CLOSED, NOT A DEFECT.** No fix needed here; L14's residual (unchecked, reference/example pages without a verifier) is where this concern actually lives if anyone wants to chase it. |
| L17 | ★ **`bitmap-transparent-compositing/webgpu` differs from its committed `sha256`, and it cannot be the L15/L16 acquisition-path mechanism under any reading — that mechanism is gated on `backend === 'webgl'`, and this is `webgpu`.** Found as a byproduct of confirming L15's withdrawal for the 3 genuine L9 scenes: `builder` compared observed-vs-committed `sha256` for all 8 columns across the 3 scenes (no baseline writes) and found 4 differ, not the 3 the L9 certification names — `bitmap-transparent-compositing/webgl` (expected, part of L9), `bitmap-transparent-compositing/webgpu` (**not** expected, not gated by anything known to select on backend), `camera-orthographic/webgl` (expected), `effect-crt/webgl` (expected). **Whatever moved the `webgpu` column is a distinct, unexplained cause** — "`webgl`-only" is not a safe assumption about the drift target set's shape going forward. | `builder` measured observed-vs-committed hashes directly, no baseline writes: `bitmap-transparent-compositing` — `webgl` `7760d99d033d` vs committed `9cd7e849ea4c`, `webgpu` `c09895b8f473` vs committed `a91e178e0119`; `canvas` and `dom` columns match. | **UNEXPLAINED, NOT YET ASSIGNED.** Nobody has looked for the cause. Whoever picks this up should treat it as its own investigation rather than folding it into the `webgl`-focused L9/L15 work — the mechanism that explains the three `webgl` differences by construction cannot explain this one. |
| L18 | ★★★ **THE WEBGL DRIFT BISECT IS COMPLETE — 34 SCENES WITH AN EXACT ADJACENT-COMMIT EDGE, 1 WITH A STATED GAP. Say "34 + 1," not "35" — that distinction must travel with the count, not three paragraphs below it.** Manager, accepting the bisect: "34 have exact adjacent-commit edges. One does not... Record it as 34 + 1, not 35." Of the 35 scenes in the corrected target list (the 39-scene sha-changed count minus the 4 proven-stale-sha of L10), every one traces to an intentional correctness fix, not accidental regression — 34 with proof, 1 (bucket D) with a best-supported read the bisect itself could not close to one commit. `builder3` bisected each to an exact adjacent-commit edge (or as close as a clean pair permits) with parent/child hash confirmation and a runtime-reach proof, not inference from a commit message. **Five buckets, cross-checked 30+2+1+1+1 = 35:** **(A) 30 scenes** — `fix(render-gl): preserve sRGB backgrounds in linear scenes`, exact edge `82b6a8da4→b467652e8`; 20 active material scenes, 4 lights, 3 scene-transform scenes, `camera-orthographic` (L9), `render-texture`, `shading-globe`; functional pipeline now passes explicit `linear` and the active-target background clear decodes sRGB into linear; material-prefix control landed exactly on prediction (24 material scenes: 22 changed matching scope, 2 unchanged as expected). **(B) 2 scenes** — `env-ibl`, `env-skybox`, `fix(rendering): consume required texture and camera fields`, exact edge `8a9ae1753→803d1cf6e`; parent/child hashes exact both sides. **(C) 1 scene** — `effect-crt` (L9), `fix(effects-gl): count crt scanlines down the image, and check the invariant in source`, exact edge `4531de011→f8f77c15f`; a real scanline-direction bug fix, not a CRT-specific test artifact — `builder3` flagged not to overclaim beyond that read. **(D) 1 scene** — `bitmap-transparent-compositing` (L9) — **the one gap in an otherwise clean bisect**: implicates an intentional two-step API migration (the Sprite/Bitmap merge at `2c825bcc0`, then a fixture migration that makes the scene reach the new path), but no runnable adjacent good/bad pair exists across the transient broken-fixture window between them — ~~best-supported read is intentional migration, not accidental drift, honestly reported as unbisectable to one commit rather than forced to look closed.~~ **REFUTED, NOT SOFTENED TO UNCERTAIN — see L26.** `builder2` found the runnable good/bad pair this bucket said did not exist: at `2c825bcc0` (the Sprite/Bitmap merge), parent and child both already read today's value, identical. The merge is refuted as cause. The scene was never in the `webgl`-only population this bisect was scoped to in the first place — it changed on both backends, so the `webgl`-gated hypothesis could never have explained it under any pairing. Manager: "I accepted 'best-supported read: intentional migration' and it is now measured false." **(E) 1 scene** — `render-target-node-2d`, `fix(scene2d): reset pooled views and orient GL targets`, exact edge `e96611bf5→7bea0e5bc`; runtime reach proven directly (`RenderTexture` → `Sprite` → `renderIntoGlRenderTexture` → `drawGlSprite` detecting `RenderTargetTextureSourceKind`). **All three of L9's genuine scenes are explained** by this bisect (A, C, D respectively). ~~none of the 35 are unexplained drift~~ **CORRECTED, NOT SOFTENED — see L28.** L18's 35 is 30 webgl-only + 5 both-GPU, not a 34+1 misfit. Four scenes closed here on exact webgl-side edges — `env-ibl`, `env-skybox` (bucket B), `render-target-node-2d` (bucket E), `material-custom-shader` (inside bucket A) — also moved on `webgpu`, and a `webgl` edge cannot account for a `webgpu` movement however exact the `webgl` parent/child hashes are. Their `webgl` sides remain genuinely and exactly explained; only the `webgpu` component is unaccounted for. | `builder3`'s bisect, no baseline or support artifact modified or recaptured anywhere in the process. Capture-mode control re-run clean alongside it (6 scenes × 2 invocations, 12 byte-identical pairs). Tree tracked-clean at `b0fb8470b`. | **CAUSE-FINDING CLOSED. RECAPTURE IS NOW THE CORRECT REMEDY, RULED BY MANAGER, BUT NOT YET AUTHORIZED — three things gate it, in this order.** Manager: "the 35 baselines are stale relative to correct behaviour, and refreshing them is the honest act rather than the laundering one... But not yet." Gates: **(1)** L12's split-write fix must land — recapturing before it manufactures a fresh crop of split records, the exact thing this investigation spent the afternoon diagnosing. **(2)** L12's provenance recording (already in flight, additive) — so refreshed records carry what produced them. **(3)** the 2026-07-03 Approved-but-Open decode-before-hash item — **this is the one that changes the order**, because it re-baselines *every* `sha256` in the suite, not just these 35; landing it after a 35-scene recapture would mean recapturing everything twice, so it lands **before** the recapture pass, not after. Then **one** authorized recapture pass, cause recorded per scene, no scene-by-scene refreshes while work is in flight. The tolerance question stays closed — no retuning, and there is no argument for it now: the coarse tier's misses were exactly the 3 genuine L9 scenes, all explained. Manager is telling the user this is coming (recapture-everything is a materially different event than "refresh 35 scenes") rather than asking, since the user's standing prohibition named size baselines specifically, not regression fingerprints. L17 (`bitmap-transparent-compositing/webgpu`) remains open and outside this bisect's scope — a `webgpu` difference cannot be explained by any of buckets A–E, which are all `webgl`-side changes. |
| L19 | ★ **CORRECTED — the blank-webgpu finding does NOT apply to the committed decode path.** ~~Decode-before-hash's current design would silently blank all 193 webgpu baselines if wired as-is — a well-formed digest of nothing, not a failure the throw-on-failure guard could catch.~~ `builder`'s original probe tested the wrong target: a live-scene-canvas blit re-read (`drawImage` the scene canvas into a fresh 2D context, `getImageData`), which was only ever a contingency `builder2` had floated in prose, never built. `builder2`'s actual committed mechanism (`09d9ddb62`) decodes the **PNG bytes Node already holds** — fetch the data URL, `createImageBitmap`, `drawImage`, `getImageData` — and never touches the GPU surface at all. Re-measured against the real diff, all four channels: **every column decodes non-blank, `webgpu` included** (`shadow-skin-nonuniform` 1926 non-zero on both `webgl` and `webgpu`; `bitmap-perbitmap-smoothing` 1887 on both; `bitmap-smoothing` 597/597/630). `builder`'s own words: "READ THE COMMIT BEFORE MEASURING AGAINST IT... a probe aimed at the wrong design produces a result that is perfectly true and completely misdirected." **What survives and is real:** the acquisition-time-reread contingency (never built) genuinely does return a well-formed blank on every `webgpu` column — that option is now known-dead, which is useful. And manager's general point stands independent of this specific scene: *"a new derivation path must inherit the existing sanity guards, not just the happy path"* — `builder2`'s throw-on-failure guard validates a 64-hex digest, which a hash of a blank image satisfies perfectly, so a `not-uniformly-empty` assertion (reusing `captureEntry.ts:195-198`'s existing `OBSERVE_BLANK_COVERAGE` threshold and vocabulary, not a new one) is being added defensively even though today's committed path doesn't currently need it as an emergency fix. **Also worth keeping, independent of this row's correction:** *"determinism is not correctness — a stable wrong answer passes every repeatability check ever written."* Two captures of a blank image are byte-identical too; the capture-twice-and-compare control this investigation has leaned on all day proves stability, not that the compared value means anything — a limit on the control, not a reason to drop it. | `builder`'s re-measurement, all four channels, against the actual diff rather than its prose description. Cross-checked against `git merge-base --is-ancestor`: `09d9ddb62` was never an ancestor of `quimby/base`, confirming it never reached anyone. | **NOT A LIVE DEFECT IN THE COMMITTED MECHANISM. THE WIRING HAZARD IT WAS FOUND ALONGSIDE WAS REAL AND IS SEPARATELY CLOSED.** `09d9ddb62` had in fact wired the new hash into all three live call sites (replacing the old encoded-bytes hash inline) — a genuine 574-value cry-wolf hazard per manager's atomicity ruling — but it never left `builder2`'s clone and was never offered to integration. `builder2` unwired it (`d0f0f8c62`, all three sites restored, with a comment at the old call site explaining wiring is atomic with the recapture and why). Mechanism stays landed and unwired, tested, green. **The defense-in-depth guard is now landed too** (`d1dd28c93`): reuses the same coverage computation as `measureObservedCanvasCoverage` and the same `OBSERVE_BLANK_COVERAGE` threshold, passed as a parameter rather than imported specifically to avoid a wiring cycle once `captureEntry` imports the module. Below-threshold refuses to hash and throws. **Honest gap reported alongside it, not hidden**: one of two mutation controls doesn't fire — deleting the in-page blank check leaves all 8 tests green, because the test double returns the `'blank'` sentinel directly and never exercises the in-page computation (a browser-only closure, no Node-testable copy without testing a duplicate that isn't the code that runs). HANDLING is verified; DETECTION is unexercised until a real capture runs — same limit as the decode itself, documented in the test file header. Also added: a premultiplication/colour-space caveat on the doc comment (`drawImage` into a 2D canvas can apply alpha premultiplication and colour-space conversion — the digest stays deterministic and tracks the render, but isn't byte-identical to the PNG's decoded samples). |
| L20 | ★★★ **THE SAME DEFECT, THREE LAYERS DEEP — CONFIRMED DESIGN: PER-FIELD PROVENANCE, NOT A SHARED OBJECT.** Continues L12. `builder4` found, and manager confirmed: a single column-level provenance object describing two independently-written fields (`fingerprint`, `sha256`) cannot establish that they agree — a later write of either field replaces the shared object, leaving the counterpart's provenance stale, so the comparison "do they agree" becomes vacuous (one value compared with itself). Concrete and already true today: `validate` records a top-level `sourceHash`; `capture` records `sha`-side provenance — different granularity, not hypothetically uniform. **The general form, stated so a fourth instance doesn't happen**: *"a value describing N independently-produced things needs N values. One value describing two separately-written fields cannot establish agreement between them — it can only report itself."* Occurrences kept climbing after this row was written, confirming the pattern rather than exhausting it. Baseline count: the two baseline fields (L9/L10/L12's original finding); the acquisition path that produced each (L14/L15); the provenance of each write (this row). **Fourth**: `builder` applied the rule to their own just-committed `CaptureBaselineProvenance` immediately after the broadcast and found the identical shape — one column-level provenance member stamped only by `captureEntry`, sitting beside two independently-written fields, with a doc comment claiming it describes "them" plural. Caught before it reached `quimby/base` (verified by content, zero hits), fixed to two named members (`fingerprintProvenance`/`sha256Provenance`) rather than a map, so the absent case reads as per-field UNKNOWN and "both carry provenance" is a literal expression instead of an inference — `builder4` was about to build the narrowed guard against the old shape and could not have expressed manager's rule against it (a single value cannot differ from itself). **Fifth, flagged not opened**: `builder2` found the *pre-existing* `sourceHash` field carries the same shape — its own header says it "identifies the scene source captured by that fingerprint," one value asserting a relationship between `fingerprint` and `sha256` that may be written at different moments. Not investigated (explicitly `builder4`'s centralization territory, scope discipline held rather than opening it "since I was in the neighbourhood") — recorded here for whoever next touches `sourceHash`'s semantics. | Manager's design reasoning, built directly on `builder4`'s finding and the concrete granularity mismatch already present in the code. | **CONFIRMED, ASSIGNED TO `builder4`.** Per-field provenance is not merely correct where the shared-object model was vacuous — **it makes the guard strengthen on its own over time**: legacy partials (no provenance) stay allowed, unchanged; but *new* partials carry provenance on the field they have, so when the counterpart is written later the pairing becomes checkable. The "absent provenance → allow" concession shrinks automatically instead of staying a permanent hole — strictly better than the shared model, which could never verify a partial-to-paired transition at all. **Two costs to state in the design note, not hide:** (1) doubles the provenance surface across 448 columns (`builder` appended provenance last specifically to keep baseline diffs minimal; per-field doubles what appends) — accepted, the alternative is a field that cannot do its job, but say so or someone "tidies" it back to a shared object and recreates this. (2) **Migration item, not blocking this landing**: `validate`'s top-level `sourceHash` and `capture`'s `sha`-side provenance need to eventually converge on the per-field model, or the guard inherits the granularity disagreement it exists to detect. **Sequencing note**: the recapture writes both fields fresh, so a suite recaptured after this lands comes out fully provenanced in one pass — landing it before the recapture is the one moment the entire suite converts at once. ★ **"Partial" is a LIFECYCLE STAGE, not a degraded state — this sharpens the case for per-field independent of the legacy backlog.** `capture --update-baseline` always runs before any validation pass touches a new scene, so every new scene starts `sha256`-only by construction: `capture → sha256-only → validate → paired` is the design, not decay. A partial record is stage one of two, not an incomplete stage two — which raises the stakes on narrowing: the over-conservative guard would not merely refuse 123 legacy transitions, it would refuse **the normal second step of every scene authored from now on**, a steady-state population replenished by ordinary work, not a shrinking backlog. **The guard should reason about the TRANSITION, not completeness**: *both fields present* is not the trigger; *both fields present with provenance that disagrees* is. With per-field provenance, the `sha256` written at stage one already carries its own provenance, so when `validate` writes `fingerprint` at stage two **the pairing is checkable at the moment it forms** — the whole future population verified at the point of joining, not by archaeology later. **LANDED — `builder4`'s narrowed provenance-aware guard, `4c0b1540e`, one clean patch over the L21 revert.** Verified, whole-repo check green, attested, no baselines touched, sent to integration independently. Capture-thread loan complete for both `builder4` (returning to Stage 4(B)) and `builder` (dry-run against the staged three-case fixture is next, once this reaches their base — the piece that actually tests the refusal logic, distinct from L21's tree-is-unblocked verification). |
| L21 | ★★★ **THE OVER-CONSERVATIVE GUARD IS LIVE ON `origin/develop` RIGHT NOW — not a future recapture risk, a present production defect blocking capture/validate for the whole suite.** `builder` measured it directly against a throwaway `--root` (no committed record touched): `capture --update-baseline` on an existing column fails with `refusing partial baseline write... cannot update sha256 while fingerprint exists`, 0 captured / 1 failed, repo baseline `md5`-verified byte-untouched. **The blast radius is the whole suite, not the 123 partials L12 named** — `setBaselineField` (`baselineStore.ts:102-118`) is the SHARED write path for both writers (`captureEntry` writes `sha256` through it, `captureValidation` writes `fingerprint` through it), and refuses whenever the OTHER field already exists, unconditionally. So: 325 paired columns (writing either field throws), 117 `sha256`-only (writing `fingerprint` throws), 6 `fingerprint`-only (writing `sha256` throws) — **all 448 existing columns can no longer be re-baselined or re-validated through the production path. Only a brand-new scene's very first write succeeds.** | `builder` measured directly on the real tree, not inferred: exact error string, exact call, exact byte-verification that nothing committed moved. | **URGENT, ESCALATED TO MANAGER.** The authorized recapture pass IS a `--update-baseline` run over existing columns — it would fail on every one under this guard, so narrowing must land before the recapture pass, which was already understood, but this is broader: anyone running an ordinary capture/validate on an EXISTING scene today hits this live on `develop` and would read it as their own breakage, not as this defect. Not a criticism of `builder4` — the narrowing was already ruled and confirmed needed; this is the blast radius measured on the real tree for the first time. ★ **RULED — REVERT NOW, DO NOT WAIT FOR THE NARROWED VERSION.** Manager reversed their own "narrow it" ruling: "I treated a live blocker in shared history as a design question. It is both, and the design question does not have to be answered before the blocker is removed." `6ff5bc719`'s refusal is reverted from `origin/develop` immediately (assigned to `builder4`); the recapture freeze is the interim protection instead, and it is strictly better because it stops writes rather than breaking them — nothing writes baselines while the freeze holds, so the split defect this guard exists to prevent cannot recur during the revert window, and manager controls when the freeze lifts (only after the narrowed guard re-lands). **Sequencing, unchanged except the guard is out of the tree meanwhile instead of blocking it**: per-field provenance (L20, in flight) → narrowed guard re-lands → dry-run → recapture. **Manager's own counting error, named for the record**: they ruled off "123 partial columns" when the actual population is "every column with any field already present" (all 448) — counted by the property they were thinking about (partial vs paired) rather than the property the code branches on (does the other field exist), the same shape as the `targetKind`-vs-backend miscount earlier, this time made in a ruling rather than a report. **CLOSED — reverted fast, as an independent parcel.** `builder4`'s `25bc9fa3b`, sent to integration standalone within minutes, focused `baselineStore` 14/14 green — removes only the blanket counterpart refusal, preserves the atomic pair/sanity writer and per-field provenance. The in-progress narrowed guard (`076d78863`) was deliberately kept OUT of this parcel and preserved separately, so the urgent unblock could land without waiting on unfinished work. **VERIFIED THROUGH THE SAME PRODUCTION-PATH INSTRUMENT THAT CAUGHT THE BLOCK, per manager's explicit ask** — a unit-test pass proves the same different claim it always does. Before (base `fb6e386bc`, guard present): `capture --update-baseline` FAILED, 0/0/1, "cannot update sha256 while fingerprint exists." After (base `4a83d3eb8`, guard reverted): 1 captured / 0 changed / 0 failed. Same command, same fixture, only the revert differs; repo baseline `md5`-verified byte-untouched both times; guard absence confirmed by content (0 hits for the refusal string), not by SHA. **Bonus, not asked for**: `builder` checked what the write produced rather than stopping at the exit code, and found their per-field provenance code exercised for the first time on an already-paired column — `sha256Provenance` stamped correctly (real `targetKind`, not a default), `fingerprintProvenance` correctly absent, exactly the unknown-per-field semantics the eventual narrowed guard's allow branch needs. **Explicitly does not test the narrowed guard** (doesn't exist yet) — shows the tree is unblocked, not that the eventual refusal logic is correct; the staged three-case fixture is still the instrument for that. |
| L22 | ★★★ **RECAPTURE CONDITIONALLY AUTHORIZED — the culmination of L12/L18/L20/L21.** Manager verified the tooling state directly in their own base rather than trusting the summary: `captureScreenshotHash.ts` present, `hashCaptureScreenshotPixels`'s single occurrence in `captureEntry.ts` is `builder2`'s comment holding the switch ("wiring this is atomic with the recapture, not before it"), not a call — held exactly as ruled. **When the staged three-case dry-run reports clean, the recapture is authorized without a further round-trip.** Wiring `hashCaptureScreenshotPixels` and running the recapture are **one operation, not two** — landing the wiring alone would make all 574 committed values wrong rather than stale, reddening every check until the recapture completes. | Manager's direct verification of the held-switch state, plus the design chain this authorization rests on: L18's 34+1 bisect, L20's per-field provenance, L21's verified revert and narrowed guard. | **FOUR CONDITIONS, ALL REQUIRED, assigned to `builder`**: (1) the dry-run's three cases (refusal on genuine split, allow on partial-to-paired, byte-identity on unchanged content) all report clean; (2) byte-verify repo baselines *before* starting, so the starting state is recorded not assumed; (3) report the count of changed baselines — expect roughly all 574, since decode-before-hash touches every `sha256`, not just the 35 drift scenes; (4) provenance fields populated on *every* rewritten record — this is the first production exercise of that code at scale, stop if any record lacks it. **The discriminator that separates "expected" from "something else happened"**: `fingerprint` is unaffected by decode-before-hash (already computed from decoded pixels), so post-recapture fingerprints should be unchanged *except* on the 35 known-drift scenes — a fingerprint moving on any other scene means something other than the expected hash change occurred, and is the one result that should stop the pass rather than be recorded. **Stop-and-escalate conditions**: a fingerprint moves outside the 35; any record written without provenance; the changed-baseline count materially below the full suite (wiring didn't take); the guard refuses a write mid-pass. Anything else — proceed and report. **After it lands**: Stage 4(B)'s render-path migration unblocks — the gate was always that the recapture must happen on a tree whose only rendering changes are the five known fixes, so a new red becomes attributable again once baselines reflect current correct behaviour. ★ **PRE-FLIGHT: THE FINGERPRINT-MOVES-OUTSIDE-THE-35 STOP CONDITION CANNOT FIRE AS SPECIFIED — a check that cannot fail is not evidence, same shape as always, caught before running anything.** `capture --update-baseline` never touches `fingerprint` at all — `captureEntry.ts:561` (the only capture-path write) writes `sha256` only; `fingerprint` is written exclusively by `validate` (`captureValidation.ts:622`). So all 439 committed fingerprints would come out byte-identical regardless of what the render actually does, even catastrophically wrong — "unchanged" is guaranteed by the code path, not by the render agreeing, and would have read as the pass's strongest safety signal while proving nothing. Compounding: **no enumerated "35 known-drift scenes" list exists anywhere in `agents/`** — the nearest thing is an unenumerated note about a small WebGPU antialiasing-tolerance set. **`builder`'s fix, cheap and non-writing**: `validate --report` (`bin.ts:202`) prints per-column regression distance and writes nothing — run once before the pass, once after, diff the distances. Any column whose distance moves is a render that actually changed, measured against committed fingerprints, touching no record — and needs no list of 35, since each scene compares against itself before/after (antialiasing-driven drift is identical both times and cancels out). Sent to manager for a ruling on adding the two passes; `builder` is holding, has run nothing. **Conditions 2 and 4 pre-measured while waiting**: 580 columns / 228 files, 574 with `sha256` (exactly the expected count), 439 with `fingerprint`, **0 with either provenance field today** — confirming this is provenance's first production exercise at scale; the pass must take `sha256Provenance` from 0 to 574. **Two mechanical notes on the flip itself, not blockers**: `hashCaptureScreenshotPixels` is `async(page, screenshot, blankCoverage)` against a synchronous held line — more than a token swap, straightforward since the page is still open; it also throws on a blank decode, a new failure mode (correctly threshold-aligned to `builder2`'s guard) that could produce a handful of pass failures unrelated to the guard itself. **RULED — adopted, and sharpened to a binary condition.** Manager owned the flaw precisely: "the most dangerous proxy is one that keeps passing" (integration's line, three hours earlier) — "I wrote one into the stop conditions of the very pass it was meant to protect." The `validate --report` before/after pair is added to the authorized procedure. **Sharpening: expected result is ZERO MOVED COLUMNS, not "movement only on the 35"** — the recapture re-records hashes, it does not change what renders, and `builder`'s own control already proved rendering is deterministic (12/12 byte-identical). So every column's distance should be identical before and after, including the 35 drift scenes; any movement at all is a finding, binary, no exceptions list to interpret. **Condition 4 now has a concrete number**: the pass must take `sha256Provenance` from 0 to exactly 574 — anything less is the stop. Full procedure: (1) `validate --report`, capture distances, non-writing; (2) byte-verify starting state, already done; (3) flip `hashCaptureScreenshotPixels` and run the recapture as one operation; (4) `validate --report` again, diff — zero moved columns expected; (5) confirm `sha256Provenance` count is exactly 574; (6) report changed-`sha256`-baseline count, expect 574 not 580. **AMENDED, TWICE, BEFORE THE PASS RATHER THAN DURING IT — both catches from accepting builder3's particle fix (L-particle) as complete.** (1) The 574 discriminator is stale: the new colored-translucent acceptance scene/route adds columns that were never in the original 574 count, so a pass reporting more than 574 is no longer automatically a failure signal. Condition restated as **574 + N changed** (N = 1, confirmed by `builder3`: their new `particle-emitter-3d` WebGL twin adds exactly one previously-absent column; the modified WebGPU route reuses its existing column. **RULED — report three SEPARATE counts, never a sum**: 574 existing columns changed, 1 new column created, 575 columns carrying `sha256Provenance` afterward (0 → 575). "575 changed" is ambiguous (misreadable as 575 *existing* columns changing, impossible since only 574 exist) and hides which component moved — the exact failure this investigation has spent the day chasing in other people's numbers. Each has a distinct wrong answer: ≠574 changed → wiring didn't take or took somewhere unexpected; ≠1 new → an unauthored scene appeared or a real one didn't; ≠575 provenance → the production exercise didn't fully fire. 580 changed remains the specific alarm. (2) `npm run support` regeneration is added EXPLICITLY to the procedure, step 4 of 7, **after the recapture and before the final gate run** — the original authorization never mentioned it, so as written the pass would have landed with `support:check` red (correctly, since the new route makes generated support stale and refresh is forbidden before the recapture) and left the runner to decide on their own judgement whether to regenerate, inside a pass where nothing is supposed to be discretionary. Full seven-step procedure: (1) `validate --report` baseline; (2) byte-verify; (3) flip + recapture as one operation; (4) `npm run support`; (5) `validate --report` again, diff, zero moved columns expected; (6) confirm `sha256Provenance` count; (7) report changed-baseline count against 574+N. `builder` holds pending N; full authorization otherwise stands. **Rollback procedure stated explicitly, detector vs backup in the right order**: the baseline files are committed, so `git checkout` restores them in one command with no external dependency — that is the backup. The per-column manifest and per-file sha256 recorded outside the repo is a *detector* (tells you something differs), not a backup (cannot restore anything). If the pass goes wrong: `git checkout` the baselines first, then diff against the external manifest to confirm the restore was faithful. |
| L23 | ★ **The builder/builder3 "changed" vs "uncovered" disagreement is resolved — coverage without naming the field is meaningless, the same shape one more time.** `scene-skinning/webgl` and `scene-skin-morph-compose/webgl` have **never** carried a committed `webgl` fingerprint — confirmed by full `git log -p` history, the string never appears. `builder`'s "changed" was a `sha256` comparison (their standing instruction, correct); `builder3`'s "UNCOVERED" was a fingerprint-coverage measurement (also correct: nothing committed to cover). One column, two independently-written fields, two instruments reading different ones — not a tree difference, and no comparison of trees was ever going to settle it. **Not a one-off**: across all 580 committed baseline columns (functional + examples), 433 carry both fields, 141 `sha256`-only (each capable of producing this exact apparent disagreement), 6 `fingerprint`-only, 0 neither. | `builder` measured the full population directly, cross-checked against `builder3`'s independent fingerprint-coverage read. | **CLOSED, and produced a free cross-check for the recapture (L22).** 433 + 141 = 574, exactly L22's condition-3 count — confirms "574 columns carry `sha256`" is the correct reading of that number. If the recapture reports 574 changed, that is full agreement; **if it reports 580, the wiring also minted `sha256` on the 6 fingerprint-only columns — a different event, not a success, and `builder` will stop and report rather than count it.** |
| L24 | ★ **Two different claims, not one — manager's own correction to how this row reads.** *"The arena fix: PROVEN — mutation-proved three ways at the time (base index no longer per-draw collapsing both bars, single-row indexing vanishing the rigid bar, the shadow pass forced to bind pose going red independently), none of that retracted. Its ONGOING regression lock: PARTIALLY INERT."* Reading this row as "there is a regression lock on this" understates the gap and overstates the protection — an inert gate reported as a live one is exactly the defect this fleet has spent two days retiring. **The two undetected mutations, by name**: forcing the consumed arena base to 0 at both write sites is masked by an identity-matrix palette; freezing the arena cursor so it never advances is masked by the L7/L8 deferred-texture-retirement fix. `scene-skin-two-skeletons`'s regression lock does not detect either — found by `builder` auditing their own scene, not assumed working.** Forcing the consumed arena base to 0 at both write sites, and separately freezing the arena cursor so it never advances, each leave the frame byte-identical and the oracle passing. **Measured causes**: the 80-joint skeleton's palette is built with `scaleX=1` on its bound joint and defaults elsewhere, so the entire palette is the identity matrix — base 0 + joint 70 = texel 280 = row 1 column 24, inside that same identity region, so a wrong base reads an identical matrix. The frozen-cursor case is masked by the deferred-texture-retirement fix (L7/L8): growth retires instead of destroying, so a bar whose bind group was recorded against the pre-growth texture keeps sampling correct data for the rest of the frame. **What the scene DOES still lock, verified on hardware**: the allocator hands out distinct bases and the second skeleton lands on row 1 (measured: base=0 joints=2, base=256 joints=80) — the multi-row path is exercised by a real render. **The scene's own comment is false as written**: it claims a base/index-math disagreement would make the bar's width wrong. It does not. | `builder` reproduced the mutation numbers directly while working integration's row 12, found both mutations pass, traced each to its specific masking cause with exact texel/row arithmetic rather than asserting. | **FIX IDENTIFIED, HELD FOR A RULING ON TIMING.** Fix: give the 80-joint palette a distinguishing joint so a wrong base changes the width — this changes the scene's render and its baseline, blocked by the standing recapture freeze regardless of timing. `builder`'s recommendation, not yet ruled: fix *after* the recapture lands, as its own isolated commit with its own single-scene baseline — specifically not folded into the decode-before-hash recapture pass, where its baseline change would be indistinguishable from the 574 expected ones. **RULED, split in two.** The false comment (no baseline consequence, alters no render) lands TODAY as its own commit — manager: "it is the class we have been deleting all day... it should not wait behind a recapture for weeks," explicitly not bundled with the palette fix, since bundling is what would put it behind the freeze needlessly. The palette fix stays held for after the recapture, own commit, own single-scene baseline, exactly as builder recommended — manager: "I would have ruled it the same way unprompted." |
| L25 | ★ **A merged commit's subject asserts wiring that its actual content does not contain — found while reconciling a rebase, verified by diff not by stat.** Commit `2a3a81d78`'s subject reads "hash a screenshot decoded pixels instead of its encoded bytes," but its conflict resolved to `upstream` during a rebase, and its actual content is only the two new module files — zero wiring in `captureEntry.ts`. The trio's net effect is correct (mechanism stays unwired, as required by L18/L22's atomicity ruling); the subject is what's wrong. **Hazard, not cosmetic**: since wiring changes what `sha256` *means* (574 committed values go from stale to wrong the moment it's live), anyone auditing whether the atomicity requirement is honored by scanning commit subjects — a reasonable check — would read this as "already wired three commits ago" and be misdirected away from the fact that the recapture is still owed. | `builder2` diffed the commit against `captureEntry.ts` specifically after noticing the rebase-conflict resolution, rather than trusting the diffstat or the subject. Confirmed independently: 0 calls to `hashCaptureScreenshotPixels`, 3 original `createHash` sites intact. | **NOTED, FIX NOT YET RULED.** `builder2`'s proposed cheap fix: a line in the capture doc stating this commit is unwired despite its subject, rather than rewriting shared history. Sent to manager for a ruling on that or something stronger. Until resolved, do not trust subject-line scanning to verify the wiring state — verify by content (grep for the actual call site), which is what caught this and what every dry-run report in this thread has already been doing. **RULED — doc note, not a history rewrite.** Manager: rewriting shared history to fix a subject is disproportionate, costing everyone a re-parent for a wording problem. Note added to `agents/packages/tool-capture/status.md`'s Open section, pointed at the code rather than restating it — `captureEntry.ts:495` already carries the authoritative comment holding the switch and the reason underneath it, so the doc note routes a reader there instead of creating a second copy that can drift. **The general rule, worth more than the fix**: *audit atomicity by content, never by subject* — a commit subject describes intent at authoring time, and conflict resolution, rebase, and squash can all change what a commit contains while leaving what it says untouched, the same verify-by-content discipline applied all day to SHAs, arriving in the one place the artifact is prose. **Named as the third form of one defect today**: a note that says what was done while the thing it describes moved underneath it — a baseline hash that didn't say what produced it (L9–L12), an attestation whose green didn't say what tree it ran on (the parcel-delivery thread), and now a commit subject that doesn't say what it carries. The artifact has to carry its own provenance, or a reader gets a confident answer to the wrong question. |
| L26 | ★★★ **L17's "webgl-only mechanism, so webgpu is unexplained" premise doesn't hold — it isn't backend-selecting, it's baseline-age asymmetry. And it partly REFUTES L18's bucket D.** `builder2` investigated L17 on a different machine, read-only, zero baseline writes throughout, and first confirmed the original finding exactly (same hashes, different device — rules out driver/environment cause). **Then measured the population L17 never measured**: the whole functional suite shows `webgl` 39/167 changed, `webgpu` 8/158 changed, `canvas` 0/82. L17 named 1 of 8 changed `webgpu` columns; the other 7 (`bitmap-smoothing`, `effect-bokeh-dof`, `env-ibl`, `env-skybox`, `material-custom-shader`, `node-blend-modes`, `render-target-node-2d`) were never investigated. **Cross-tab bears directly on L18**: of the 8 `webgpu`-changed scenes, 5 also changed on `webgl` — including `bitmap-transparent-compositing`, L18's bucket D. Of the 39 `webgl`-changed, 34 are `webgl`-only, exactly L18's "34 with an exact adjacent edge." `bitmap-transparent-compositing` was never in the `webgl`-only population the bisect implicitly ran against — the `webgl`-gated migration hypothesis could not have explained it under any pairing, because it changed on both backends. `builder2` bisected the `webgpu` side and found a real, runnable edge — `c7d91b9b3` "fix(rendering): tactical 2D color-space compatibility" (07-31) *restored* the render to today's value; at `2c825bcc0` (07-28, the Sprite/Bitmap merge L18 implicated), parent and child both already read today's value, identical. **That is precisely the runnable good/bad pair L18 said did not exist, and it refutes the merge as cause** — something transiently broke the render between 07-28 and 07-31, and `c7d91b9b3` is a fix, not a break. **Root cause of the apparent `webgl`-vs-`webgpu` asymmetry**: the 07-18 recapture (`7383f696a`) rewrote `webgl` 127/127 but `webgpu` only 1/119 — `webgl` compares against a 07-18 baseline, `webgpu` against 07-01, a 17-day evidence-age gap producing exactly this pattern with zero backend-selecting mechanism. **Why every gate stays green**: fingerprints haven't been rewritten since 2026-06-25 (predates the `sha256` field); `validate` reports a *perfect* 0.00 regression for `webgpu`, not within tolerance — exact. The whole drift is sub-fingerprint, invisible to the coarse gate. | Independent reproduction on a second machine; full-suite measurement (not the 8-column sample L17 worked from); the `2c825bcc0` and `c7d91b9b3` edges each measured as an exact adjacent pair, not inferred. **Self-caught method error worth keeping**: the bisect loop initially assumed its lower endpoint was old without measuring it — testing it moved the answer from a nonsense commit (`a4c388649`, an unrelated feature) to the real one (`c7d91b9b3`); an unmeasured bisect endpoint is a fabricated data point, and it fabricated one at exactly the position that would have named a false culprit. | **UNDETERMINED, NOT YET ASSIGNED.** `webgpu` today equals its 07-28 value, but the committed baseline is from 07-01 — the edge that *first* moved it lies somewhere in 07-01..07-28, and that window is unbisectable: `tool-capture` doesn't exist that early, every probe dies `ERR_MODULE_NOT_FOUND`. The control most wanted — capturing at the commit that wrote the baseline, to check environment reproduction — isn't runnable. **`builder2`'s recommendation, sent to manager**: the 7 unexamined `webgpu`-changed columns are the live item, not this one scene, and "`webgpu` matches baseline" is an *untested* proposition repo-wide (118 of 119 columns never checked, gate blind to sub-fingerprint drift) — this scene is simply where someone happened to look. Not blocking the L22 recapture (fingerprint-side, a different axis from the `sha256`-only decode-before-hash pass). **RULED — the 8 must be named here BEFORE the recapture consumes the evidence, and they are: `bitmap-transparent-compositing`, `bitmap-smoothing`, `effect-bokeh-dof`, `env-ibl`, `env-skybox`, `material-custom-shader`, `node-blend-modes`, `render-target-node-2d`.** Manager: "changed" means the current render differs from the committed `sha256`; the recapture rewrites every `sha256`, so after the pass all 8 match by construction — the signal that would prompt investigation is consumed by the very operation about to run. The information survives in git history and this report, but stops being visible to anyone who didn't read it, which is how a finding becomes folklore. **Root cause restated as a population defect, one level up from L20's per-field provenance**: the 07-18/07-01 recapture asymmetry means the whole `webgl`/`webgpu` baseline *population* has mixed provenance, not just individual records — baselines of different ages compared as though contemporaneous. Per-field provenance (L20) does not address this; it fixes agreement-checking within one record, not age-consistency across the population. **L18's bucket D is REFUTED, not left uncertain — see L18.** **The "34 = 34" claim needs a membership check, not a cardinality one** — sent to `builder2`, set difference both ways: same members confirms the `webgl`-only scoping was accidentally correct and bucket D was the one non-member; different members means an unexplained scene exists in one set and not the other. **Explicit unknown, stated so it can't be misread as clean**: "`webgpu` matches baseline" is untested repo-wide, not verified — the absence of `webgpu` findings currently reads as `webgpu` being clean, and that is the same absence-of-evidence error this investigation has hit four times today. |
| L27 | ★★★ **THE RECAPTURE RAN AND THE STOP CONDITION FIRED — L22's authorization worked exactly as designed.** Committed (`0b6ea218a`), not landed; `builder` remedied nothing on their own judgment. All three counts missed, each with a located cause: (1) existing changed 569 vs target 574 — exactly the 5 columns whose recapture failed (blank decode ×2, verifier-didn't-terminate ×2, `wgpu`-buffer-timeout ×1), still holding OLD encoded-byte hashes while every other column now means decoded pixels — self-identifying by missing `sha256Provenance`. A 6th failed then succeeded on retry (flaky, not deterministic). (2) new columns 2 vs target 1, and neither is the named scene — the target was structurally unreachable on `builder`'s base (`builder3`'s particle-emitter-3d WebGL twin hadn't landed there); the 2 that appeared (`node-blend-modes-fixed`/`webgpu`, `per-node-effect-lane`/`webgpu`) are unrelated scenes gaining a `webgpu` column for the first time. (3) `sha256Provenance` 576 vs target 575. **The L22 580-alarm's substance fired even though the number didn't reach it**: `sha256` was minted on 5 of the 6 declared-`sha256`-less columns, independently caught by `fingerprint-source-hashes:check` (5 violations) — a committed gate refusing a change nobody authorized. **Discriminator**: `functional` 331/331 zero movement, clean exactly where the 12/12 determinism control was actually measured; `examples` showed 2 apparent moves, but the larger one (`examples/bitmap/canvas` 0.00→3.45) does not reproduce on re-measurement, and the suite's measured column set is independently unstable run-to-run (2 present→absent, 10 absent→present) — it cannot support a binary zero-movement rule regardless of this pass. | `builder`'s committed-not-landed pass, full accounting given before any remediation, matching the L22 procedure step for step. | ★ **RULED — remediate on the commit, then land; do NOT revert.** Manager: "569 real conversions are worth keeping... reverting throws away expensive correct work to fix five columns." **Rec 1, agreed**: strip the 5 minted `sha256`, restore the declared sha-less shape. **Rec 2, REVERSED from `builder`'s own recommendation**: strip the 5 failed columns' `sha256` too rather than leaving them wrong-but-flagged — "a wrong value that requires a companion field to detect is worse than an absent value. Absent is true. Wrong-but-flagged is a trap for anyone who reads one field." Unrepresentable beats detectable, the standing rule this thread has enforced all day; fingerprint-only is a state that already exists and is representable, and these 5 get recaptured once the underlying bugs are actually fixed. **The real finding, to be recorded as such**: two of the five were committing hashes of *blank frames* until today — decode-before-hash did not cause this, it *exposed* it, because hashing decoded pixels with a blank guard finally refused what encoded-byte hashing silently accepted. **Rec 3, agreed, and manager owns the cause**: the +1 target was manager's own "whose base" error — applying `builder3`'s N=1 to a tree that doesn't contain the twin, hours after broadcasting against exactly this error class. Withdrawn; 574/1/575 is satisfiable only on a tree carrying the twin. **The 2 unexpected columns: do not strip blindly** — one question first, not an investigation: did `webgpu` genuinely gain the ability to render these scenes (real capability, keep), or did the capture run a different renderer set (harness noise, strip)? **The discriminator's `examples`-suite failure was manager's own scoping error**, not a defect in the result: the zero-movement rule was only ever validated against `functional` (where the determinism control was measured), and should never have extended to a suite whose column set isn't stable. Scope it to `functional` (331/331, genuine) and record explicitly beside it that the comparison only covers columns with a committed fingerprint — 141 `sha256`-only columns were skipped, and "zero moved" says nothing about them. **What `builder` did that manager "would not have thought to require"**: held the comment-fix commit (`2ef278823`) until after captures finished, since editing mid-pass would have changed that scene's `sourceHash` while the served bundle was still pre-edit — stamping provenance describing a source that didn't render, the precise defect this thread exists to make unrepresentable — then re-captured the one affected scene afterward so provenance matches committed source. "Stopping with a red gate visible rather than quietly greening it is the whole reason this is recoverable." ★ **SELF-CORRECTED before manager could act further: 3 of the 5 "failed" columns are NOT scene defects — they are false positives in the hash function itself.** `render-target-axes.webgl.ts`, `render-target-format-policy.webgl.ts`, and `render-pass-shared-context.webgl.ts` each render a full-canvas solid colour *on purpose*, declare `minCoverage = 0` explicitly, and say so in their own source. `functionalVerify.ts:219-222` honours that declaration and **passes** these scenes; `captureEntry.ts:502` then calls `hashCaptureScreenshotPixels` with the **global** `OBSERVE_BLANK_COVERAGE = 0.001`, rejecting the same frame the verifier just approved, milliseconds apart in the same capture. The two blank checks inside one pass disagree by construction — one respects the scene's declared threshold, the other ignores it, and the information that would resolve it (the scene's own `minCoverage`) is already on the entry and already used ten lines earlier. **These 3 columns can never be recaptured while this stands** — not transient, they fail identically on every future pass, so "strip and recapture once the underlying bug is fixed" does not apply as originally ruled; there is no scene bug, the fix is in the hash function (pass the entry's own `minCoverage`, or reuse the already-computed verification result, instead of the global constant) and needs its own ruling as a code change inside the pass under review. **What is unaffected**: the other 2 of the original 5 (`camera2d`/`dom`, `scene3d`/`webgl`, both verifier-stall) plus `skeleton`/`webgpu` (buffer-map timeout) are a genuinely different, unexplained-by-this class. **A second, smaller finding from the same look, also held for a ruling**: the hash computes *before* the screenshot and logs are written, so a failing capture leaves no screenshot and no logs — the artifact most wanted for diagnosing a hash failure is exactly what the failure suppresses. One-line reorder (move the writes above the hash) fixes it without changing any recorded value or hash semantics. **RULED, both items, and manager retracted their own amplification of the wrong claim in the same breath.** "Two were committing hashes of blank frames" was withdrawn in full — manager traced the actual defect to their own guard specification: they required reuse of "the existing threshold," meaning the global `OBSERVE_BLANK_COVERAGE`, when the right existing thing was the scene's own declared `minCoverage` — "I was guarding against vocabulary divergence and specified a threshold divergence instead. Same defect, different axis, introduced by the requirement written to prevent it." **Guard fix authorized** (entry's own `minCoverage`, or the already-computed verification result, replaces the global constant) — small, local, ruled directly. **Then RECAPTURE the 3, do not strip**: the fix touches the guard, not the hash — a frame that passes produces the same value either way, so this doesn't touch the 569, it converts 3 refusals into successes and changes nothing else. Strip was for columns that cannot yet be recaptured; these can, immediately. **The other 3 failures (`camera2d`/`dom`, `scene3d`/`webgl` verifier-stalls, `skeleton`/`webgpu` buffer-timeout) stay separate — retry before stripping**, not strip-on-first-failure: `render-pass-shared-context` already succeeded on retry once, evidence the class is flaky rather than deterministic. Fail identically on retry → then strip, and at that point they're real defects. **Logging-order fix also authorized**, general shape recorded for recurrence: "a diagnostic written after the step that can fail is a diagnostic that exists only when you do not need it" — cost real time here, since the frames that would have shown 3 scenes correct were never written. Full sequence: guard fix → recapture the 3 → retry the other 3 → logging reorder → land. **EXECUTED — all four items landed clean, one new blocker surfaced that touches the freeze.** Guard fix: passing `null` when verification already passed (rather than inventing a second threshold) — all 3 minCoverage refusals became successes first try, confirmed touching only the guard, not the 569. Logging reorder: done, writes precede the hash. Retry-before-strip: 2 of 3 were flake (`camera2d/dom`, `skeleton/webgpu`, passed on retry, `sha256` restored), 1 is real (`scene3d/webgl`, failed identically twice more — meets the bar for a genuine defect, stays stripped, needs an owner). ★ **New blocker, not anticipated by the ruling**: recapturing `render-pass-shared-context/webgl` — authorized — re-reds a *different* committed gate. That column is a named entry in `FINGERPRINT_SOURCE_HASH_ALLOWANCES` (`scripts/check-fingerprint-source-hashes.ts`), valid only when both `sourceHash` and `sha256` are absent; writing the authorized `sha256` invalidates the allowance declaration. `builder` checked the tempting fix (copy the provenance's embedded `sourceHash` into the top-level field) and correctly refused it — top-level `sourceHash` means "source when the *fingerprint* was captured," the provenance one means "source when the *sha256* was captured," different events by definition; copying one into the other manufactures false agreement between two independently-written things, the original split-provenance defect one layer down, dressed as a gate fix. Three options sent to manager, none picked unilaterally: (a) strip the fresh `sha256`, gate green, contradicts the explicit no-strip ruling; (b) authorize a single-scene fingerprint write (`validate --update-fingerprints`, this scene only) — honest and permanent, removes the allowance as resolved, but touches a fingerprint, outside everything authorized so far and inside the freeze; (c) leave it red, land nothing. `builder` recommends (b). Committed not landed (`03a01e1b2` adds to the chain); `npm run check` has exactly 1 red gate, this violation; attesting fail rather than landing red. **RULED — option (b), scoped precisely, this is the final piece.** The freeze protects evidence while the drift investigation is open; this column is not in the 35, not in the 8 webgpu-changed, carries no open finding — the freeze's reason does not reach it, and applying it past its purpose would be the same error corrected all day elsewhere. (a) strips a value that is now correct to preserve a declaration that is now obsolete: the allowance existed because the column lacked provenance, and writing genuine provenance resolves the exception rather than needing to keep it. Authorized: `validate --update-fingerprints`, this scene only, writing `fingerprint` + genuine top-level `sourceHash`; then remove the `FINGERPRINT_SOURCE_HASH_ALLOWANCES` entry as **resolved**, stated as such rather than as inconvenient. Two mandatory requirements: verify no other column's fingerprint moved, before and after (the single-scene scope is a claim to confirm, not a setting to trust); state the resolved-not-inconvenient reason explicitly wherever the removal is recorded. `builder`'s null-threshold guard fix (deferring to the already-computed verification result rather than inventing a second source of the same fact) is better than what was originally authorized. Their refusal of the sourceHash-copy shortcut is named the best judgement in the whole thread — "the hardest place to see" the split-provenance defect is inside a one-line fix that makes a red gate green. ★ **OPTION (b) TURNED OUT IMPOSSIBLE, NOT MERELY HARD — the fingerprint writer refuses this scene for the identical reason the hash guard did.** `validate --update-fingerprints` on this column returns "not baselined — uniform frame... a blank capture must not become ground truth" — `isUniformCaptureFingerprint` rejects the same full-canvas solid colour. Since top-level `sourceHash` is written beside the fingerprint, and the fingerprint write is refused, (b) has no path under any authorized pass, now or ever. Requirement 1 verified against a full pre-snapshot (not inferred from "0 written"): 0 of 582 columns moved either field. **New fact surfaced**: the column's *currently committed* fingerprint is itself dead evidence — 1 distinct cell of 256, a legacy value predating the uniform-refusal guard, which the system would refuse to write today. Zero discriminating power, exactly what the guard exists to prevent — the allowance's premise is inverted: `sha256`+provenance is real freshly-captured evidence; the fingerprint is a value the system itself would no longer accept. **Tested and reverted, nothing committed**: dropping the uniform fingerprint (keep `sha256`+provenance) and removing the allowance as resolved makes `fingerprint-source-hashes:check` exit 0, confirmed, then restored from external copies and verified `git status` clean. **Escalated rather than acted on** — deleting a committed fingerprint is bigger than removing a registry line, squarely freeze territory, and the third time tonight the easy gate-green path would have destroyed or manufactured evidence. Awaiting manager's authorization on dropping the dead fingerprint (builder's recommendation) versus fallback (a) stripping the fresh `sha256` instead. **RULED — authorized, and this closes the recapture.** "The freeze protects evidence. A fingerprint with 1 distinct cell out of 256 is not evidence — it is a value that matches any uniform frame. It cannot fail, therefore it cannot detect, therefore deleting it removes nothing that works." Keeping it is worse than dropping it: an always-passing fingerprint reports coverage the column doesn't have — the most-dangerous-proxy shape this investigation has spent two days retiring, applied to itself. The column ends up better instrumented, not worse: from a fingerprint that can't fail to a fresh `sha256` that can, carrying provenance. Commit message states both halves per requirement 2. Once it commits, the full chain lands (`2ef278823`, `0b6ea218a`, `91a6ec795`, `03a01e1b2`, plus this piece) — decode-before-hash wired, 574 converted, provenance 0→full, support regenerated, guard divergence found and fixed inside the same pass. **See L29 — closed at 0 additional, chain landed. |
| L28 | ★★★ **THE 34=34 MEMBERSHIP CHECK CONFIRMS MANAGER'S SUSPICION — L18's closing line ("none of the 35 are unexplained drift") is too strong for 4 more scenes, not just bucket D.** `builder2`'s webgl-only-34 and L18's 34-with-an-exact-edge overlap at 30, with 4 different members each side. In L18's 34 but not `builder2`'s: `env-ibl`, `env-skybox`, `material-custom-shader`, `render-target-node-2d` — all four changed on **both** GPU backends. In `builder2`'s but not L18's: `effect-displacement`, `effect-glitch`, `effect-scanlines`, `effect-screen-space-fog` — L18 correctly excluded these as L10's 4 proven-stale-sha. **The agreement at 34 was coincidence of two different subtractions over two different populations** — exactly the failure manager named before the check ran. **What survives**: the underlying bisect work is sound — bucket A reproduced exactly (30 members including 20 material scenes matching L18's 20), and `builder2`'s 39 `webgl`-changed = L18's 35-scene target + the 4 L10-stale effects, independently confirming both L18's count and its subtraction. **The actual scope of L18's 35: 30 webgl-only + 5 both-GPU, not 34-webgl-only + 1 misfit.** `bitmap-transparent-compositing` (bucket D) is only one of the five both-GPU scenes. The other four — `env-ibl`, `env-skybox` (bucket B, closed on exact edge `8a9ae1753`→`803d1cf6e`), `render-target-node-2d` (bucket E, closed on `e96611bf5`→`7bea0e5bc`), `material-custom-shader` (inside bucket A's edge `82b6a8da4`→`b467652e8`) — each **also** moved on `webgpu`, and each was closed only on a `webgl`-side exact edge. A `webgl` edge cannot account for a `webgpu` movement, however exact the `webgl` parent/child hashes are. **The irony**: bucket D, the one L18 honestly flagged as unclosed, is now the one whose `webgpu` side *is* explained (L26, `c7d91b9b3`); the four L18 closed outright are the ones still missing a `webgpu` explanation. `material-custom-shader` confirmed by measurement, not arithmetic (44 material scenes, 20 `webgl`-changed, exactly 1 `webgpu`-changed), specifically to avoid repeating the deduction error being corrected. **Full picture of the 8 `webgpu`-changed columns** (L26): 5 sit inside L18's 35 (the four above + `bitmap-transparent-compositing`); 3 are entirely outside it, owned by no bisect anyone has run — `bitmap-smoothing`, `effect-bokeh-dof`, `node-blend-modes`. | `builder2`'s set arithmetic over data already collected — zero new captures, zero baseline writes, tree clean throughout. | **RULED (manager-b6806196) — CORRECTED IN L18, DO NOT SOFTEN.** L18's closing line is amended in place naming the four: `env-ibl`, `env-skybox`, `render-target-node-2d`, `material-custom-shader`. **Full picture of the 8 `webgpu`-changed columns (L26), 7 now standing unexplained**: `bitmap-transparent-compositing` (bucket D) is explained — its `webgpu` side traces to `c7d91b9b3`, per L26. The other 7 do not: the 4 named above (webgl side exact, webgpu side unaccounted) plus 3 entirely outside any bisect ever run — `bitmap-smoothing`, `effect-bokeh-dof`, `node-blend-modes`. **These are explanation gaps, not live defects** — the recapture rewrote every `sha256`, so all 7 columns match current behaviour; what is missing is why they drifted, not that they are wrong today. **No dispatch** — manager: "record now, chase later." Left here, named with evidence pointers (L18's bucket edges for the 4; L26's full-suite measurement for the other 3, unbisected), for whoever next has slack rather than assigned. The scene L18 honestly flagged uncertain (bucket D) is the one now explained; the four marked settled are the ones still missing an explanation — worth carrying forward as its own lesson, not just this correction's mechanics. |
| L29 | ★ **How many other committed fingerprints are uniform (1 distinct cell), reporting coverage they don't have? Population never measured — found by stumbling on one, exactly the day's recurring shape.** L27's `render-pass-shared-context/webgl` fingerprint turned out to have 1 distinct cell out of 256 — a legacy value from before `isUniformCaptureFingerprint` existed, which the system would refuse to write today. Manager: "that guard prevents new ones — it does nothing about the ones already committed. Every committed fingerprint with one distinct cell is a gate that cannot fail, reporting as coverage." Same shape as the stale-dist finding (2→9) and the drift count (31→39) — one instance found by accident, population never counted. | Not yet measured. | **ASSIGNED TO `builder`, cheap and read-only — distinct-cell count per committed fingerprint across all 582 columns, no writes.** If the count is 1 (just `render-pass-shared-context`), this closes as a singleton. If more, do **not** delete any of them on this pass — bring the number and the list back for a ruling; manager: "if it is twenty, the support matrix is claiming coverage on twenty columns that cannot detect anything, and that is a finding on the scale of the fingerprint-tier misses." **RESULT — CLOSED AT ZERO, with a near-miss worth a separate note.** 438 fingerprint columns examined, read-only, nothing written: uniform (1 cell) — 0, the one retired in L27 was the only one. Distribution: 1-2 cells 1 | 3-4 cells 7 | 5-8 cells 45 | 9-16 cells 97 | 17+ cells 288. ★ **The single "1-2 cells" column is a near-miss, not a reassurance**: `functional/bitmap-downscale-smoothing/canvas` is 254 cells of `0x808080` and 2 cells of `0x7f7f7f` — a 1-LSB difference in 2 of 256 cells, non-uniform on a technicality (the guard is a strict all-cells-identical test). It has almost no power to distinguish a correct render from a blank one, and is fragile in the other direction — a 1/255 difference is exactly what a rounding change or rasterizer revision flips, so it can fail for reasons unrelated to render correctness. Worst of both: nearly unfalsifiable and easy to trip. Not touched, per scope (count-and-list only). **Open question, explicitly manager's**: should the guard measure spread rather than strict cell-distinctness? Not part of this pass. **RULED — do not change the guard, record the distinction instead.** builder's analysis stands: nearly unfalsifiable and fragile, worst of both. Manager's sharper framing: "the guard tests REPRESENTABILITY (is this uniform?) and its purpose is POWER (can this discriminate?). Those are different properties, and the name suggests the second." But no spread threshold is being chosen on one example — "some low-cell scenes may legitimately be near-uniform (a two-tone test being the obvious case); picking a cutoff now would invent a number with no evidence about what is legitimate," the same trap avoided three times tonight. **Candidate set flagged, not acted on**: the 8-column low tail (1-2 cells: 1, 3-4 cells: 7) from the census distribution. Revisit only if one of them produces a confusing result — "then we would have an instance rather than a threshold." |
| L30 | ★ **L20's guard cannot fire in production today — its own precondition is unreachable.** `assertMatchingProvenance` (baselineStore.ts:170-179) returns early unless BOTH fingerprintProvenance and sha256Provenance are present, only throwing when both exist and disagree. But nothing writes the fingerprint half: captureEntry.ts's sha256 write passes a provenance object (captureEntry.ts:561), captureValidation.ts's fingerprint write does not (captureValidation.ts:622, provenance arg omitted) — and `setBaselineField` treats a missing provenance argument as `clearCaptureBaselineProvenance` (baselineStore.ts:117-121), actively erasing the field rather than leaving it unset. builder's own measurement over all 582 columns post-recapture: 571 carry sha256Provenance, 0 carry fingerprintProvenance. Same shape as tonight's recurring rule, this time self-applied by the author against their own landed work: a check that cannot fail is not evidence. The design (per-field provenance, one value per independently-written thing) is sound and stands; what does not exist is the second write that would let the guard ever compare two real values. | builder measured fingerprintProvenance/sha256Provenance presence across all 582 committed columns directly; confirmed independently by foreman reading baselineStore.ts:170-179, captureEntry.ts:561, and captureValidation.ts:622 against the cited line numbers |  **AWAITING A DECISION** — completing this means captureValidation.ts stamping provenance on its fingerprint write, the same way captureEntry.ts does for sha256. builder flags the blast radius as unmeasured and possibly large: the moment fingerprintProvenance starts being written, the guard begins comparing it against the sha256Provenance already committed on 571 columns, and any column where the two were genuinely captured under different conditions — the entire premise of the recapture arc — starts refusing writes. builder's recommendation, escalated rather than acted on: measure first, via `validate --update-fingerprints` against a scratch `--root` with the guard in record-only mode, counting how many columns would refuse, before wiring anything against committed records. builder explicitly declined to run that measurement unauthorized the night a freeze just closed. **UPDATE — implemented, committed `5f47db484`.** captureValidation now stamps fingerprint provenance at the moment it writes the fingerprint, sourced only from the SAME capture run that produced it (`captureWorkflow.ts:209` was carrying `fingerprints` through to validation without their provenance — the root cause of L30's original gap, now fixed with a comment at the site). **Design choice that changes the completion's shape**: one provenance object is built per capture and shared by both the sha256 and fingerprint writes — the two fields cannot disagree on a fused run because there is only one set of conditions recorded, not because a guard catches a mismatch. That is the 'one writer for both fields' half of L20's original design call, reached from the other end. Manager's write-and-stamp-together constraint is enforced in code: a fingerprint this pass did not capture gets no provenance, by construction. Verified by foreman: `captureWorkflow.ts:209` diff matches exactly as described, patch series (6 commits, newest `5f47db484`) present in full under `handoff/in/received/builder-2dc81c09/commits/` — **correcting the parcel's own header, which misreported 'received 0 patches (note-only)'; six patches arrived, self-caught via directory check per the fleet's mandatory receipt-readback practice.** **Measurement attempted, returned an uninformative zero, self-caught before being reported as a result**: standalone `validate --update-fingerprints` samples via `loadFingerprint`, not the capture path, so it produces no provenance at all — 450 scratch columns, 0 with both provenance fields, 0 disagreements logged because the guard never had two values to compare. Reporting that as a low disagreement rate would have argued manager's prediction wrong using an instrument that could not have detected it. **Reshapes the risk being sized**: only the FUSED `batch` path (capture → validate in one run) populates both halves; on a fused run the two fields agree by construction (guard cannot fire); a fingerprint-only run writes no provenance (guard cannot fire either). The one shape that CAN disagree is narrower than 'mixed-age population': a fused run with `update-fingerprints` on and `update-baseline` off, where a fresh fingerprint provenance meets an already-committed sha256 provenance from a different run. **Foreman authorizes the fused-path variant directly** — same envelope already granted by manager (scratch `--root`, guard in record-only/logging mode, no committed baseline touched), only the specific subcommand corrected because standalone `validate` structurally cannot produce the data; no new policy question is opened by the substitution. Builder to bring the per-column count and differing fields, scoped now to the narrower fused-run-only disagreement case rather than the original mixed-age-population framing. **MEASUREMENT RESULT — 399 of 399 judgeable columns disagree, 100%.** Fused `batch --only=functional`, scratch `--root`, `--update-fingerprints`, `update-baseline` off, guard logging not throwing, per the authorization above. Of 450 scratch columns: 399 carry both provenance fields (the only ones enforcement could judge) — 0 agree, 399 disagree; 5 carry fingerprint-only, 46 carry sha256-only (uniform/unstable columns that got no fingerprint). Manager's HIGH-rate prediction is confirmed. **But the cause is not mixed ages — every one of the 399 differs in exactly one field, `frames` (fingerprint=1, sha256=0); `sourceHash`, `targetKind`, `verifyPublished`, `warmupFrames` disagree in zero of 399.** An age-driven population would show `sourceHash` moving, since that is the field that tracks the scene changing; it is silent across the board. **Root cause, traced end to end and independently spot-checked by foreman against source**: tonight's recapture ran `capture --tool=functional` with no `--frames`; `tool-capture.batch.json` passes `--frames=1` for the functional subject (confirmed, line 7); `captureWorkflow.ts:174` — confirmed verbatim, `Math.max(1, captureOptions?.captureFrames ?? 1, validationOptions?.captureFrames ?? 1)` — makes the batch leg capture at `frames: 1` regardless. With `update-baseline` off, the sha256 half kept tonight's `frames: 0` while the fingerprint half was written at `frames: 1`. **A systematic configuration difference between the standalone capture pass and the batch pass, not an age problem** — deterministic, affects every column, carries no information about whether the two values describe the same render. **The split the guard detects is real**: those 399 fingerprint/sha256 pairs genuinely were produced by captures configured differently; before tonight the record could not express that at all, and now does, in 399 places — the completion is working as designed, the uncomfortable part is that switching on enforcement today would refuse nearly every write. **Builder's recommendation**: (a) make the two passes agree on `frames` — a configuration fix, not a code one, and the real remedy; (c) record-without-enforcing in the interim (where the code already sits). **Builder explicitly refuses (b) — excluding `frames` from the comparison — unless overruled**: `frames` genuinely changes what renders, dropping it would make the guard blind to a real difference to get a pass, and the 100% rate is the guard correctly reporting a genuinely mixed-configuration population rather than being miscalibrated. Foreman agrees with builder's refusal of (b) on the merits. **Choice between (a) and (c), and the timing/sequencing of (a) against the fleet's base-delivery gap, is manager's call, not foreman's — escalated.** Hygiene: guard restored byte-identical (md5 `b590037a…`), tree clean, no committed baseline touched — scratch root only. Builder also self-caught and corrected the same receipt-header miscount flagged in this entry's prior update, this time before being told: checked the directory (64 patches) rather than asserting from the note's shape. **RULED (manager) — SEQUENCE (a) BEFORE COMPLETING THE STAMPING, NOT (c) ALONE.** Manager's own read of the result: the 100% rate confirmed the HIGH-rate prediction, but `sourceHash` disagreeing in zero of 399 refuted the reasoning behind it (mixed ages) — "the number confirmed my prediction and the mechanism refuted my reasoning," recorded as the sharper lesson than the number alone. **Completing the fingerprint-provenance stamping now, before fixing the `frames` mismatch, would manufacture the defect the guard exists to detect** — every column would get `frames:1` on the fingerprint half against `frames:0` already committed on the sha256 half, disagreeing by construction on day one. Order ruled: fix the `frames` disagreement between the standalone and batch capture paths (builder's option (a)) FIRST, then complete the stamping — records written after the fix agree from the start, and the guard becomes a live check rather than a switch nobody can flip. **Not urgent**: production never writes `fingerprintProvenance` today, so the 399 disagreement is what would happen, not what is — (c), record-without-enforcing, is simply what the code already does, staying that way by inaction rather than decision. **Safety basis for the fix, cited by manager, not independently re-verified by foreman**: all 238 frames-sensitive columns are byte-identical between `frames=0` and `frames=1` — the disagreement is real in the record and immaterial in the pixels, making (a) correct hygiene rather than urgent repair, safe in either direction. **Timing**: do it when the base-delivery gap (this entry's escalation trail) resolves and there is a natural window — a capture-path config change immediately after a freeze closed, with the fleet's base stale, is the wrong moment for a non-urgent change. **Builder's refusal of (b) recorded as theirs, unprompted, by manager's own words**: "excluding the one field that disagrees, in order to make the comparison agree, is changing the instrument so the result goes away — the move we have refused on the tolerance, on the baselines, and on the size pins." **STANDING, NOT DISPATCHED** — queued for whoever has the base restored and a natural window; not part of tonight's active work. **PROVENANCE OF THE 238 FIGURE, CLARIFIED BY BUILDER** — it is builder's own measurement from earlier tonight (the widened instrumented run answering the two gating capture questions), not an unsourced citation: 238 frames-sensitive columns by `targetKind`, all byte-identical across `frames=0` and `frames=1` screenshots. But it was measured on the PRE-recapture tree — 569 columns since had their `sha256` rewritten (encoded PNG bytes → decoded pixels), 2 added, 3 removed, 5 stripped, population now 579 not 580. **The direction likely survives** (two byte-identical screenshots hash identically under either scheme, so decode-before-hash doesn't disturb that specific finding); **the count and membership do not** — which columns are frames-sensitive was derived from a column set that no longer exists in that exact shape. Not safe to quote "238" as current if the frames fix comes to rest on it; safe to rely on as directional evidence that the difference is immaterial in pixels. Builder offers to re-measure (cheap, read-only, same instrumented shape) when the base-delivery window opens — not run now, per the same not-tonight ruling. **Builder also sharpens the (b) refusal recorded above**: not a general law that `frames` never matters — the guard should keep comparing it — but a population-specific finding that for THIS set it demonstrably doesn't, which is exactly why the (a)-first sequencing is safe rather than merely permitted. |
| L31 | ★ **`examples/scene3d/webgl`'s verifier stall (L27, stayed stripped, flagged "needs an owner") is a verifier-handshake defect, not a render defect — narrowed while standing by, unowned and unassigned before this.** `builder`, read-only, no code change, no baseline written: `capture --tool=examples --filter=scene3d --renderer=webgl --no-verify` captures first try, `0 failed`, and the frame itself — inspected directly, not inferred from the exit code — shows a fully correct scene (cube, sphere with correct specular highlight, cone, ground plane, cast shadows). With the verifier in the loop the same page fails identically every time ("render verifier did not reach a terminal state"). **Narrows the ownership**: WebGL-only (the WebGPU column of the same example passes verification and carries full `sha256`+provenance today); zero page logs on every failing run, consistent with a verifier target registered late or not at all rather than one erroring against a broken render. **Consequence worth carrying so the stripped `sha256` isn't misread**: the column was correctly stripped under L27's retry-then-strip rule (failed identically twice on retry), but the underlying scene is not broken — the value is recoverable the moment the verifier-registration handshake is fixed, no render work needed. | `builder`'s own `--no-verify` capture plus direct frame inspection, contrasted against the verified-failing verified path; not independently re-run by foreman | **NOT ACTED ON, by design** — builder explicitly declined to touch verifier registration while standing by ("fixing a verifier registration is someone's owned work... squarely the kind of capture-path change manager just said to hold until the base-delivery gap clears"). Still unowned; recorded here with the scope narrowed so whoever picks it up starts at the verifier registration for this example on WebGL, not at scene3d rendering. **RESOLVED AND LANDED — `271c1a211`, by `builder2`.** Confirmed, not inferred: probed the live page for `window.__ftVerification`, found it NULL the entire run — nothing ever registers, so of L31's two readings (late-registered vs missing entirely) it is MISSING ENTIRELY. A passing sibling (`scene-globe/webgl`) publishes `state:passed` under the identical probe within 5s, so the publisher itself works; `scene3d` simply never reaches it. **Root cause**: `scene3d` is the only example whose `enterFrame` ends with an unconditional `requestAnimationFrame`, so the page never settles — every sibling ends with `if (!captureMode) requestAnimationFrame(enterFrame)`. Confirmed by foreman directly against source: pre-fix `scene3d/src/app.ts` has zero `captureMode` references; `scene-globe/src/app.ts` has the guard at three sites, matching pattern. **Fix, verified both directions**: added the sibling guard verbatim (`+4/-2`, one file) — without it 1 failed, with it 0 failed on `webgl`, `webgpu` stays byte-unchanged. Whole-repo `npm run check` exit 0, zero baseline writes. **Not a capture-pipeline change** — the entire fix is inside one example's `app.ts`; foreman's stop-and-check condition was never reached, flagged explicitly by builder2 rather than assumed clear. **Named-but-unmeasured, stated as such rather than as understood**: why `webgpu` tolerates the identical unconditional loop. One structural fact behind it, confirmed by foreman directly against source — `captureBrowser.ts:109` intercepts only `webgl`/`webgl2`/`experimental-webgl` contexts; `webgpu` is never intercepted, so the two backends run genuinely different harness paths. That makes the asymmetry unsurprising but not explained — which step of the `webgl` interception path the forever-loop actually breaks was not measured. **Two durable tooling gaps surfaced, worth a row on their own, not filed as fixed**: (1) `captureEntry.ts:414` — confirmed verbatim, `if (verification?.state !== 'passed') throw new Error('render verifier did not reach a terminal state')` — is a collapsed-verdict oracle covering both registered-and-stalled and never-registered under one message; `captureValidation.ts:421` already distinguishes them ("verifier registered but stalled…", confirmed verbatim) but `captureEntry` doesn't use that distinction, which is what sent earlier attempts hunting a stall that wasn't happening. (2) observe mode cannot diagnose this class — its diagnostics block sits downstream (`captureEntry.ts:536`) of the throw it exists to explain (`:414`), which is why 3 observe-mode runs yielded nothing on this exact defect. Neither gap fixed here; recorded for whoever owns capture-tooling next (`agents/packages/tool-capture/status.md` is already over its stated budget, not grown further by foreman for this). **The earlier logging-reorder/zero-page-logs hypothesis is refuted, not merely unconfirmed**: the passing `webgpu` column also emits zero page logs, so that was never a discriminating signal. **Expect this column to report "changed" against baseline, not fail** — the baseline predates any working capture of this scene, so it's stale by construction; builder2 did not write it, correctly leaving that to the recapture/freeze process, and handed the fix to integration directly per standing practice. |



★ **L8 IS THE SIBLING LAW AGAIN, AND THIS TIME IN CODE RATHER THAN IN A RULE.** This morning a misplaced
trigger was corrected in the gate rule and its sibling in the register's add-trigger survived untouched,
because *fixing one instance does not fix its siblings and nothing announces the ones still broken*. L7
was found at one site, the remedy was scoped to a second, and **the same law says look for the rest** —
so the sweep was run rather than assumed. It found a live instance nobody had named, on the most
trafficked path of the three. **A defect discovered through one symptom gets scoped to that symptom by
default; the scoping is an artifact of how it was found, not a measurement of where it lives.**

★ **AND THE SWEEP ITSELF NEEDED THE MECHANISM CHECK, NOT THE NAME MATCH.** The first pass counted four
live sites. `bindWgpuVideoTexture` and `bindWgpuTexture` look identical to the real ones in a grep and
have **zero production callers** — one is reached only by its own test file. Had the finding shipped on
the grep, it would have named dead code as a live defect and spent someone's afternoon on it. **A search
by name produces candidates; only counting call sites produces a population.**

★ **L7 FLIPPED WITHIN THE HOUR, AND THE ROW IS THE REASON ANYONE CAN TELL.** Filed FIXABLE-not-landed at
`fd199607c`, CLOSED at `fee58db7b` when the parcel arrived — **two states, both true when written, and
the transition is legible.** Had it been filed CLOSED at the start, the file would read identically today
and would have been *wrong for the interval*, with nothing to mark which side of the arrival any reader
was standing on. **A row that can flip is worth more than a row that was right eventually.**

★ **L7 IS FILED WITH ITS FIX OPEN, AND THAT IS A CORRECTION TO THE SUGGESTED TEXT, NOT A QUIBBLE.** It
arrived proposing sub-state **CLOSED — fixed as part of the arena work**, which is true of `builder`'s
clone and false of this tree: the fix is unlanded, so a reader of merged history would go looking for a
safeguard that is not there. **A register whose rows describe merged claims must be read against merged
history, and the two diverge exactly when work is in flight.** This is the same divergence that produced
row 7's disputed counts — *a sender's reading can be true of a tree this file is not about.* The useful
consequence of splitting the row is that the DEFECT half got **stronger**: it is now verified-at-tree
rather than as-reported, because integration could check the missing sibling directly. **Where a claim
divides into a condition and a repair, they can carry different provenance, and collapsing them to one
takes the weaker of the two.**

★★ **L2's options are TWO, not three, and the third was never available.** The ruling against repair
rested on the invariant — repairing substitutes our reconstruction for the approver's text. **A
mechanical reason now stands beside it: the repair would not STICK.** An append-only ledger and a
rebase disagree about what history means — **the ledger judges the WORKING TREE while a replay
reconstructs COMMITS** — so **a fix that edits the line keeps coming back until the branch carries NO
diff on it**, and L2's base already carries one.
⇒ **So: accept it standing, or rewrite the history that removed it. "Edit it back" is not an option
that exists.**
**The join is marked because it does not fully transfer:** the observed case was a **resurrection**
and L2 is a **removal**. **The general finding transfers; the instance does not**, and saying which
is which is what keeps the narrowing honest.

**L2 is presented as a chosen state rather than an oversight.** It is the one entry here that **the
user rather than the fleet has to close**, and leaving it in a generic act-on-it category would have
invited exactly the repair the ruling forbids.

★ **L2 carries a second defect worse than itself: the append-only gate compares against
`origin/develop`, so once a mutation lands the baseline absorbs it and the gate goes permanently
quiet.** ⇒ **The gate answers *has anything unlanded mutated a guarded line* while being read as
*has a guarded line been mutated*** — the misaddressed-answer law, in a CI gate, still live. **It will
never report L2 again.**

## Permitted and unbuilt

A third state beside *permitted-and-built* and *forbidden*: a capability the rules allow, that nobody
has built, **and that nobody declined to build** — it was never considered. **The middle invites nobody
unless it has a name**, which is why it gets a table rather than a mention.

**Trigger for finding these:** enumerating what every rule permits is unaffordable, so audit the rules
that have stopped being checked and started being assumed. **The tell is that you cannot remember
deciding.**

| # | Capability | Why it went unbuilt |
|---|---|---|
| U2 | Guard modules for `font-formats` — the caller-facing half of the diagnostics inversion rule | The rule is one sentence with two halves: guards for caller-facing warnings, `explain*` queries for silent sentinels. The second half was built deliberately and remembered; **there is no memory of considering the first.** The package ships zero guards — `grep -rlE "export function enable[A-Za-z0-9]*Guards" packages/font-formats/src/*.ts` — while the same command across `packages/*/src/*.ts` shows guards are the established norm rather than a declined option — and a comment in the same package tells a caller the remedy is one line of registration, which the rule itself calls a missing guard. Which situations are caller misuse rather than bad data is a judgement the conventions doc governs, unread. |
| U1 | A durable real-font verification harness for `font-formats`, using the existing sha256-verified on-demand fetcher | *No vendored fixtures* had collapsed into *real-font verification is inherently throwaway*, so every corpus this session was scratchpad-only. **The author had built the permitted mechanism earlier in the same arc and then did not use it**, which removes every cheaper explanation. Wiring font packs into the conformance fixture system is phase-2 scope. |

## Skips inside the backed set

The whole-repo test run that backs most claims **skips two browser-contract files on every run**, and
suites requiring a browser, a GPU, or the network do not execute in the integration environment at
all. So the subsumption of a sender's focused suite is near-total rather than total, **and the
exceptions are, predictably, the expensive ones.**
